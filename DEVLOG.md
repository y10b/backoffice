# 개발 로그

이 백오피스를 만들면서 내린 결정과, 도중에 발견한 것들을 정리한 문서입니다.

> 이 저장소는 공개 저장소입니다. 작업 중 실제 네이버 세션 쿠키로 테스트했지만,
> 쿠키·API 키는 이 문서를 포함해 저장소 어디에도 기록하지 않았습니다.
> 인증 정보는 `.gitignore`된 로컬 `data/backoffice.db`에만 저장됩니다.

## 1. 요구사항

크리에이터 어드바이저에서 네이버 트렌드·주제별 인기 키워드를 확인하고, **메인 키워드 + 서브
키워드**로 제목을 잡아 글을 쓴 뒤, 같은 글을 티스토리에도 올리는 백오피스.

## 2. 초기 결정

| 항목 | 선택 | 이유 |
|---|---|---|
| 데이터 수집 | 쿠키 + 내부 API 직접 호출 | Playwright 자동 로그인은 캡차/2FA에 막히고 느림 |
| 발행 | 복사용 결과물만 생성 | 네이버 글쓰기 API 폐지, 티스토리 Open API 종료 |
| 본문 생성 | Gemini | 사용자 보유 키 |
| 형태 | Next.js 로컬 웹앱 | GUI로 키워드 조회·편집·미리보기 관리 |

의존성은 Next/React뿐입니다. SQLite는 Node 22.5+ 내장 `node:sqlite`를 써서 네이티브 빌드가
없습니다.

## 3. 깨지기 쉬운 부분에 대한 설계

> 3~5장은 크리에이터 어드바이저를 쓰려던 시절의 기록입니다. 여기서 언급하는 `naver.ts`와
> `curl.ts`는 6장에서 공식 API로 갈아타며 삭제했습니다. 왜 갈아탔는지의 근거라 남겨둡니다.

크리에이터 어드바이저는 공개 API가 아니라 경로와 응답 스키마가 예고 없이 바뀝니다. 그래서 둘 다
코드에 고정하지 않았습니다.

- **경로** — DB에 저장된 편집 가능한 프리셋. UI에서 수정하거나, DevTools `Copy as cURL`을
  붙여넣으면 [`src/lib/curl.ts`](src/lib/curl.ts)가 URL을 파싱해 바로 조회
- **응답 스키마** — [`src/lib/naver.ts`](src/lib/naver.ts)의 파서가 JSON을 재귀로 훑어
  "키워드처럼 생긴 문자열 필드를 가진 객체 배열" 중 가장 큰 것을 선택. `keyword` /
  `queryKeyword` / `query` 등 필드명 변형과 중첩 구조를 흡수하고, 실패하면 원본 JSON을 UI에 노출

## 4. 검증

- `npm run build` 통과, 서버 기동·DB 생성·API 응답 확인
- 순수 로직 29개 검증: 응답 스키마 4종 파싱, cURL 셸 인용 규칙(작은/큰따옴표, 백슬래시
  continuation, `-X` 플래그 스킵), 마크다운 변환, XSS 이스케이프

변환기 버그 하나를 잡았습니다. 마크다운 `##`이 `<h3>`로 나가 제목(h1) 다음 h2를 건너뛰었고,
`#`만 h2로 끌어올리고 나머지는 유지하도록 [수정](src/lib/markdown.ts)했습니다.

## 5. 실제 호출에서 막힌 지점 — 요청 서명

실제 세션 쿠키로 테스트하자 전 엔드포인트가 `403 {"status":"fail","message":"Forbidden"}`을
반환했습니다. 로그인 리다이렉트 HTML이 아니라 API 핸들러가 낸 JSON이라 경로 문제가 아니었습니다.

프론트엔드 번들(`/assets/index-*.js`)을 확인해 실제 구조를 파악했습니다.

모든 호출은 `` `/api/v6${path}` ``로 조립되고, 실제 경로는 이렇습니다.

```
/api/v6/accounts/channels
/api/v6/home/popular-category-keyword   ?service=&channelId=&date=
/api/v6/home/popular-demo-keyword       ?service=&channelId=&date=
/api/v6/home/weekly-recommendation      ?service=&date=
/api/v6/home/soaring-contents           ?service=&channelId=&interval=&date=
/api/v6/trend/query-demo-distribution   ?interval=&date=&keyword=
/api/v6/inflow-analysis/referrer-query-rank ?service=&channelId=&metric=&interval=&date=&ct=&limit=
/api/v6/dashboard/channel-ranks         ?service=&channelId=
```

`service`는 `naver_blog`, `channelId`는 블로그 아이디입니다.

문제는 경로가 아니었습니다. 모든 요청에 **HMAC 서명 헤더**가 붙습니다.

```
X-CA-Nonce   crypto.randomUUID()
X-CA-Ts      Date.now()
X-CA-Sig     HMAC("METHOD|...|ts|nonce", <__ca_key 쿠키에서 파생한 키>)
```

응답이 `403` + `code: "CA_KEY_INVALID"`면 `/api/v6/accounts/channels`를 다시 호출해 CA 키를
재발급받고 원 요청을 1회 재시도합니다. 그래서 **`/accounts/*`만 서명 없이 통과**하고(키 발급
엔드포인트라서), 나머지는 전부 403이 됩니다. 실제로 `/accounts/channels`는 정상 응답했습니다.

이건 스크립트 접근을 막으려고 네이버가 의도적으로 넣은 통제입니다. 서명 알고리즘을 복제해
우회하는 방향은 택하지 않았습니다.

## 6. 공식 API로 교체

쿠키 경로를 걷어내고 공식 API 세 곳으로 갈아탔습니다. 쿠키 만료도, 무단 우회도 없습니다.

| API | 얻는 것 | 인증 | 한도 |
|---|---|---|---|
| 검색광고 · 키워드도구 | 연관 키워드 + 월간 검색수(PC/모바일) + 광고 경쟁정도 | HMAC 서명 (발급받은 비밀키) | 계정별 |
| 검색 API · 블로그 | 키워드별 총 문서수 | Client ID/Secret | 일 25,000 |
| 데이터랩 · 검색어트렌드 | 검색 추이 (상대지수) | Client ID/Secret | 일 1,000 |

### 세 소스를 합치는 이유

키워드도구의 검색수만으로는 "쓸 만한 키워드"를 못 고릅니다. 검색량이 많으면 그만큼 경쟁도
치열하기 때문입니다. 그래서 검색 API로 그 키워드의 블로그 문서수를 세서
**경쟁률 = 문서수 ÷ 월간 검색수** 를 계산합니다. 낮을수록 수요 대비 공급이 적다는 뜻입니다.
공식 지표가 아니라 경험칙이라 UI에도 그렇게 적어뒀고, 절대값보다 같은 표 안의 상대 비교로
쓰도록 안내합니다.

데이터랩은 계절성·상승세를 보는 용도입니다. 다만 키워드 그룹을 5개까지만 받아서 전체 표에는
못 붙입니다. 상위 5개만 자동 조회하고, 나머지는 표에서 체크해 `선택 N개 추세 보기`로 따로
부르게 했습니다.

### 서명 (`src/lib/searchad.ts`)

5장에서 우회하지 않기로 한 그 HMAC이, 공식 API에서는 **발급받은 비밀키로 정당하게** 서명하는
방식으로 그대로 쓰입니다.

```
X-Timestamp  Date.now()
X-API-KEY    액세스라이선스
X-Customer   CUSTOMER_ID
X-Signature  base64(HMAC-SHA256(`${timestamp}.${METHOD}.${path}`, 비밀키))
```

`path`에 쿼리스트링을 넣으면 안 됩니다. `/keywordstool?hintKeywords=...` 가 아니라
`/keywordstool` 로 서명합니다.

### 응답에서 걸린 것

월간 검색수가 10 미만이면 숫자가 아니라 `"< 10"` 문자열로 옵니다. 그대로 `Number()` 하면
`NaN`이 되고 정렬이 무너져서, `parseCount()`가 9로 환산합니다. UI 각주에도 이 값이 실측이
아니라 마스킹된 값이라고 적어뒀습니다.

힌트 키워드는 최대 5개이고, 검색광고는 공백을 제거한 형태로 집계합니다. `normalizeHints()`가
공백 제거·중복 제거·5개 절단을 한 번에 합니다.

### 부분 실패 처리

검색광고가 실패하면 표 자체가 안 나오지만, 검색 API나 데이터랩이 실패해도 검색량 표는
그대로 나옵니다. 어디까지 성공했는지는 `SourceStatus[]` 로 내려서 결과 위에 배지로 붙입니다.
문서수 조회는 키워드 1건당 1호출이라 동시 실행을 4로 제한하고, 개별 실패는 그 키워드만
`null`로 남기고 넘어갑니다.

## 7. 검증

- `npm run build` 통과
- 자격증명 없는 상태에서 `/api/keywords`, `/api/trend`, `/api/settings/test` 가 모두
  500이 아니라 안내 문구를 반환하는지 확인
- 더미 자격증명으로 실제 엔드포인트를 호출해, 요청이 네이버 서버까지 도달하고 **인증
  사유로만** 거절되는지 확인 (`403 Invalid API-KEY`, `401 NID AUTH Result Invalid`).
  요청 형식·헤더·서명 경로 자체는 정상이라는 뜻입니다.

이 과정에서 403 안내 문구를 고쳤습니다. 키가 틀리면 401이 올 거라 예상하고 403은 "권한 없음"
으로 안내했는데, 실제로는 키가 틀려도 403 `Invalid API-KEY` 가 옵니다. 두 코드를 같은
안내로 합쳤습니다.

## 8. 검색 API·데이터랩이 신규 발급 중단

키를 발급받으려니 애플리케이션 등록 화면의 **사용 API 드롭다운에 `검색`과 `데이터랩`이
아예 없었습니다.** 남아 있는 건 네이버 로그인·인증서·전자문서·네이버페이·카페·캘린더·캡차뿐.

발급된 Client ID/Secret으로 그냥 호출해 봤더니 이렇게 갈립니다.

```
가짜 키   → 401 NID AUTH Result Invalid (1000)
실제 키   → 401 Scope Status Invalid
```

에러가 다릅니다. **키는 유효한데 앱에 그 API 스코프가 없다**는 뜻입니다. 신규 제휴를 받지
않는 게 맞았습니다. 이 두 케이스를 "Client ID/Secret 이 잘못됐습니다" 하나로 뭉뚱그리고
있어서, `errorDetail()`이 `scope` 문자열을 보고 갈라 안내하도록 고쳤습니다.

### 대안 — 검색광고 응답을 더 짜냈다

키워드도구는 `showDetail=1`이면 검색수 말고도 광고 지표를 줍니다. 이미 받아오면서 안 쓰던
필드로 두 지표를 만들었습니다.

| 지표 | 계산 | 뜻 |
|---|---|---|
| 모바일 비중 | 모바일 검색 ÷ 전체 검색 | 블로그 유입은 모바일이 지배적 |
| 광고 흡수율 | 월평균 광고 클릭수 ÷ 월간 검색수 | 검색 중 몇 %가 광고로 빠지는지 |

광고 흡수율이 문서수의 빈자리를 제일 잘 메웁니다. 광고주가 돈을 안 쓰는 키워드는 상업적
의도가 낮은 정보성 검색이고, 그런 검색일수록 사용자가 블로그 본문을 읽습니다. 문서수처럼
공급을 직접 세는 건 아니지만, 수요의 **성격**을 알려줍니다.

실제로 `제주도 여행` 시드에서 갈린 결과:

```
7월제주도날씨   31,960   모바일 94.5%   광고 흡수 0%
부산숙소       108,300   모바일 80.1%   광고 흡수 2.4%
```

검색량은 부산숙소가 3배지만 예약 의도라 광고가 가져갑니다.

### 정렬 순서 때문에 반쯤 헛돌던 것

새 정렬을 붙이고 결과를 찍어 보니 검색수 내림차순 그대로였습니다. 라우트의 허용 정렬키
화이트리스트에 새 키를 안 넣어서 `searches`로 폴백하고 있었습니다.

고치고 나서 더 큰 문제가 보였습니다. `limit`을 **검색수 기준으로 먼저 자른 뒤** 정렬해서,
`광고 흡수율 낮은 순`이 실제로는 "검색수 상위 N개를 흡수율로 재배열"한 것이었습니다.
948건 중 정작 흡수율 낮은 키워드는 후보에도 못 들었습니다. 자르기 전에 최종 기준으로 먼저
정렬하도록 바꿨더니 결과가 완전히 달라졌습니다 — 위의 `7월제주도날씨`가 그렇게 나온
키워드입니다.

문서수·경쟁률 정렬만은 예외로 남겼습니다. 그 값은 자른 **뒤에** 조회하는 거라 미리 정렬할
수가 없습니다.

## 9. 문서수 대신 SERP 를 본다

문서수를 살릴 수 있나 해서 검색 결과 페이지를 직접 읽어 봤습니다. 결론은 **차단이 아니라
숫자가 없다**였습니다.

```
HTTP 200, 642KB, blog.naver.com 링크 374개, title "제주도여행 : 네이버 블로그검색"
"약 N건" 표기 → 없음 (PC / 모바일 / where=blog / where=post / where=view 전부)
```

네이버가 총 건수 노출을 없앴습니다. `문서수 ÷ 검색량` 은 재료가 사라져 포기했습니다.

그 대신 페이지에서 확실히 나오는 두 가지로 방향을 틀었습니다. **"얼마나 많이 있나"(공급량)
대신 "지금 상위를 누가 얼마나 단단히 잡고 있나"** 를 봅니다.

| 지표 | 뜻 |
|---|---|
| 최신글 비율 | 상위 노출 글 중 최근 30일 내 비율. 높으면 계속 갈아치워지는 레드오션 |
| 인플루언서 비율 | 상위 노출 블로그 중 인플루언서 비율. 높으면 일반 블로그가 밀림 |

### 파서를 어디에 고정했나

결과 아이템 래퍼는 `m65iNUxtb_Effkaj` 같은 해시 클래스라 배포마다 바뀝니다. 대신 값이 든
디자인시스템 클래스 하나에만 기댔습니다.

```
sds-comps-profile-info-subtext  →  "2026.02.21." / "4주 전" / "6일 전"
```

42건이 잡음 없이 떨어졌고(절대 31 + 상대 11), 결과 수와도 일치했습니다.

인플루언서 판정에서 한 번 헛짚었습니다. `in.naver.com/{핸들}` 을 그대로 세니
`home`, `delivery`, `identity-access` 같은 네이버 내부 경로가 섞였습니다. **결과에 실제로
뜬 `blog.naver.com` 핸들과 교집합인 것만** 세도록 바꾸니 내부 경로가 자연히 걸러집니다.

### 변별력 확인

지표를 만들었으면 실제로 갈라지는지 봐야 합니다. 처음 4개로 재보니 난이도가 33~45로 몰려
있어서, 성격이 확연히 다른 키워드로 다시 쟀습니다.

```
8월제주날씨     57   최신글 90%   중앙연령 14일
맛집           52   최신글 100%  중앙연령 14일
가을축제        33   최신글 48%   중앙연령 43일
제주도여행       23   최신글 31%   중앙연령 53일
제주도박물관     17   최신글 20%   중앙연령 48일
대한민국구석구석   15   최신글 27%   중앙연령 43일
```

15~57 로 벌어졌고 해석도 맞습니다. 지금이 8월이라 `8월제주날씨` 에 새 글이 쏟아지는 중이고,
`맛집` 은 상위 100%가 최근 한 달입니다.

**두 지표가 엇갈리는 게 이 조합의 핵심입니다.** `8월제주날씨` 는 광고 흡수율 0% 라 광고
지표만 보면 좋은 키워드인데, 진입 난이도는 57 입니다. 광고 흡수율은 **수요의 성격**을,
진입 난이도는 **지금 공급 상태**를 봅니다. 한쪽만 봤으면 놓쳤을 판단입니다.

### 선은 지켰다

공식 API 가 아니라 공개 검색 페이지를 읽습니다. 다만 5장에서 거절한 서명 우회와는 다릅니다.
인증을 뚫는 게 아니라 로그인 없이 누구나 보는 페이지를 읽습니다. 그래도 크롤러는 아니게
두려고, **사용자가 버튼을 눌렀을 때만** 동시 2개·간격 0.4초·한 번에 20개 상한으로 돕니다.
마크업이 바뀌면 조용히 0건이 되지 않도록, 한 건도 못 읽으면 오류로 올립니다.

## 10. 애드센스를 달기로 하면서 지표가 뒤집혔다

9장까지의 전제는 "광고가 안 가져가는 키워드가 좋다"였습니다. 블로그 유입만 볼 때는 맞지만,
애드센스를 달면 **광고주가 돈을 쓰는 주제라야 단가가 붙습니다.** 흡수율이 0%인 키워드는
트래픽이 와도 수익이 거의 없습니다.

단가 데이터가 필요해졌는데, 검색광고 API 에 입찰가 추정 엔드포인트가 있었습니다.

```
POST /estimate/average-position-bid/keyword
  { device, keywordplus, key, items: [{key, position: 1}] }
→ { estimate: [{ keyword, position, bid }] }
```

키워드도구와 같은 서명 방식이라 그대로 붙었습니다. 블로그 유입이 모바일 위주라 `MOBILE`
기준으로 받습니다. 애드센스 CPC 그 자체는 아니지만, 같은 시장에서 같은 광고주가 그 주제에
얼마를 쓰는지를 보여주는 상관 지표입니다.

여기에 트래픽을 곱해 `수익 잠재력 = 검색수 × 단가 ÷ 1000` 을 만들고 기본 정렬로 뒀습니다.
단가만 높고 아무도 안 찾는 키워드와, 검색량만 많고 단가가 바닥인 키워드를 동시에 거릅니다.

### 분야별로 실제로 재봤다

만든 도구로 후보 분야를 직접 측정했습니다.

| 분야 | 평균 입찰가 | 평균 진입난이도 |
|---|---|---|
| 보험/법률 | 39,894원 | 21 |
| 건강 | 8,085원 | 24 |
| 재테크 | 5,038원 | 20 |
| 생활정보 | 4,711원 | 30 |
| 테크/IT | 2,273원 | 26 |

통념과 어긋나는 결과가 나왔습니다. **보험/법률은 단가가 테크의 17배인데 진입 난이도는 오히려
가장 낮은 축**입니다. 고단가면 경쟁도 심할 거라 예상했는데 반대였습니다.

이유는 광고 경쟁과 블로그 경쟁이 다른 시장이기 때문입니다. 보험사는 파워링크에 돈을 쓰지
블로그 글로 싸우지 않습니다. `교통사고합의금` 은 인플루언서 0%, 중앙연령 56일 — 오래된 글이
그대로 상위에 남아 있습니다.

`뉴스/시사` 는 후보에서 빠졌습니다. `속보` 는 연관 키워드가 0건이고, `시사상식` 은 언론사명
(한국경제·매일경제)으로, `부동산 뉴스` 는 법률 용어(낙찰·주택법)로 흩어집니다. 뉴스 수요는
"지금 그 사건"에 붙는데 그건 월간 집계에 잡히지 않고 며칠이면 사라집니다.

### 호출 한도

분야를 연속으로 조회하다 `HTTP 429 Too Many Requests` 를 맞았습니다. 검색광고 API 는 짧은
간격의 연속 호출을 막습니다. 오류 문구로 그대로 노출되니 잠시 뒤 다시 조회하면 됩니다.
입찰가는 50개씩 끊어 부르고, 후보 풀을 100개로 제한해 조회 1회당 최대 3호출로 묶었습니다.

## 11. 시각 자료를 이미지 대신 HTML 로

글에 시각 자료를 넣기로 했는데, 이미지 생성은 택하지 않았습니다.

| | 이미지 | HTML |
|---|---|---|
| 만들기 | 별도 API·저장소·업로드 | 본문과 같은 응답에 담김 |
| 옮기기 | 붙여넣기로 안 됨 | 티스토리 HTML 모드에 그대로 |
| 검색엔진 | 글자를 못 읽음 | **내용을 읽음** |

Gemini 가 `visuals` 배열로 비교표·절차·체크리스트를 만들고, 본문 마크다운의 `{{visual:N}}`
자리에 끼워 넣습니다. 네이버 스마트에디터는 태그를 대부분 지우므로 사실상 티스토리 전용입니다.

### 정제기가 필요했다

모델이 만든 HTML 을 그대로 렌더하면 **미리보기 화면에서 스크립트가 실제로 돕니다.** 통과시킬
태그와 CSS 속성을 화이트리스트로 못박고 나머지는 텍스트로 떨어뜨립니다.

테스트가 버그를 하나 잡았습니다. `<script>` 태그는 지우는데 **안쪽 내용이 텍스트로
살아남아** `alert(1)` 같은 소스가 본문에 노출됐습니다. 태그만 버리고 내용을 남기는 규칙이
스크립트류에는 정반대로 작동한 것입니다. `script`·`style`·`iframe` 은 닫는 태그까지 통째로
건너뛰도록 고쳤습니다. 26개 단언 통과.

같은 시기에 표 렌더링 버그도 잡았습니다. SEO 프롬프트가 표를 최소 1개 요구하는데
`markdownToHtml` 이 파이프 문법을 몰라서, **생성되는 모든 글에서 표가 `<p>| 구분 | 한도 |`
로 뭉개지고 있었습니다.** 정렬 표기(`:---:`)까지 지원하도록 고쳤고, 그 과정에서 대시 1개짜리
구분줄(`:-:`)을 못 읽는 버그도 함께 잡았습니다.

## 12. GA4·애드센스 연동 — 구글의 벽 세 개

성과를 회수하지 못하면 키워드 선택이 맞았는지 알 수 없습니다. 루프를 닫으러 갔다가 벽을
셋 만났습니다.

**① 애드센스는 서비스 계정을 안 받는다.** GA4 Data API 는 서비스 계정 JWT 로 되는데,
AdSense Management API 는 OAuth2 사용자 동의만 받습니다. 그래서 두 연동의 인증 구조가
아예 다릅니다.

**② gcloud 로는 애널리틱스 권한을 못 받는다.** 서비스 계정을 GA4 속성에 등록하는 걸
자동화하려고 `gcloud auth application-default login --scopes=...analytics.edit` 을 실행했더니
**"차단된 앱"** 이 떴습니다. gcloud 의 내장 OAuth 클라이언트는 검증되지 않아 민감 범위를
요청할 수 없습니다.

**③ 쿠키를 복사해도 세션이 안 따라온다.** 브라우저 자동화를 붙이려고 Chrome 프로필의
쿠키 DB 를 복사했지만 로그인이 유지되지 않았습니다. Chrome 127+ 의 **앱 바인딩
암호화(App-Bound Encryption)** 가 프로필 도난을 막습니다. Chrome 151 은 기본 프로필에서
`--remote-debugging-port` 자체도 무시합니다.

셋 다 우회 대상이 아니라고 판단했습니다. 5장에서 네이버 HMAC 서명을 복제하지 않기로 한
것과 같은 성격입니다.

### 그래서 어디까지 자동화됐나

우회 대신 **정식 경로를 자동화**했습니다.

| 단계 | 방법 |
|---|---|
| API 3종 활성화·서비스 계정·키 발급 | `gcloud` CLI |
| GA4 속성·데이터 스트림 생성 | Analytics **Admin API** |
| 서비스 계정 권한 부여 | 브라우저 자동화 (사용자가 로그인 1회) |
| 티스토리 GA 태그 설치 | 브라우저 자동화 |

속성 생성은 처음에 GA4 관리 화면을 클릭으로 몰려다 실패했습니다. 오버레이가 클릭을
가로채고 직접 URL 은 홈으로 튕겼습니다. **서비스 계정에 계정 수준 편집자 권한을 준 뒤
Admin API 로 만드는 쪽으로 바꾸니 한 번에 끝났습니다.** SPA 를 클릭으로 모는 것보다 API 가
언제나 낫습니다.

태그 설치는 실제 블로그 페이지를 열어 `googletagmanager.com/gtag/js?id=...` 요청이 실제로
나가는지까지 확인했습니다. 설정 화면에서 "사용중"으로 보이는 것과 태그가 실제로 발화하는
것은 다릅니다.

### 0 과 오류를 구분한다

태그를 막 달아서 조회수가 0 입니다. 이때 화면에 그냥 0 만 뜨면 고장난 것처럼 보입니다.
연결 테스트가 두 상태를 갈라 안내합니다.

```
연결은 정상입니다. 다만 수집된 데이터가 아직 없습니다 (태그 설치 직후면 몇 시간 걸립니다).
```

## 13. 영상 생성 도구 세 곳을 확인했다 — "무료"의 실체

유아 채널 영상을 무료로 만들 수 있다는 얘기를 듣고 세 곳을 확인했습니다. 결과가 갈렸습니다.

| 도구 | 무료 API | 실제 상황 |
|---|---|---|
| 구글 플로우 | **API 자체가 없음** | Flow(flow.google)는 웹 UI 제품입니다. 같은 Veo 모델의 개발자 경로는 Gemini API 이고 **유료** — 초당 $0.15~0.40 |
| HeyGen | **2026년 2월 폐지** | 무료 API 티어가 없어졌습니다. 유료 종량제만 남았고, 말하는 아바타라 유아 애니메이션엔 애초에 안 맞습니다 |
| Fish Audio | **있음** | `s2.1-pro-free` 가 유료와 **같은 모델**입니다. 83개 언어, 하드 캡 없음(Fair Use 적용) |

"플로우로 무료"는 **웹 UI 이야기**였습니다. Google AI 플랜 크레딧으로 브라우저에서 만드는
것이고, 코드에서 부를 수 없습니다. 그래서 두 갈래로 나눴습니다.

- **무료 경로** — Flow 웹에서 만들어 내려받고, 쇼츠 화면의 소재 업로드로 넣습니다.
  기획안 화면에 `전체 프롬프트 복사 (Flow 용)` 버튼을 둬서 붙여넣기만 하면 되게 했습니다.
- **자동 경로** — Gemini API 의 Veo. 유료라는 걸 화면에 금액과 함께 박아뒀습니다
  (8초 클립 $1.2~3.2, 8장면이면 한 편에 $10~26).

### Veo 는 Gemini 키를 그대로 쓴다

Veo 도 같은 `x-goog-api-key` 라 **이미 있는 다중 키 로테이션이 그대로 재활용**됩니다.
새 자격증명이 하나도 늘지 않았습니다. 다만 Veo 는 무료 티어에 없어서 결제가 설정된
프로젝트의 키여야 하고, 429·403 에 그 안내를 실었습니다.

기획안의 장면 초와 Veo 가 받는 초가 다릅니다. Veo 3.1 은 4·6·8초만 받아서, 기획안 값을
가장 가까운 허용값으로 맞춰 보냅니다(5초→4, 7초→6, 15초→8).

### 결과 영상은 프록시를 거친다

Veo 가 주는 URI 는 API 키 헤더가 있어야 열립니다. 브라우저는 헤더를 붙일 수 없어서 서버가
대신 받아 흘려보냅니다. 이때 **URI 를 그대로 믿으면 임의 주소로 키를 보내는 통로가 됩니다.**
호스트가 `googleapis.com` 인 것만 통과시킵니다 — `notgoogleapis.com` 같은 유사 도메인이
정규식에 걸리지 않게 `(^|\.)googleapis\.com$` 로 고정했습니다.

### Fish Audio 의 단서

무료 등급은 "일부 상용 시나리오에 제약이 있을 수 있다"고 공지돼 있습니다. 수익화 채널에
쓸 거라면 유료 등급이 안전해서, 모델을 고정하지 않고 설정에서 바꾸게 뒀습니다.

## 14. 손으로 만든 클립을 자동으로 조립한다

Flow 는 API 가 없으니 "그럼 웹 자동화냐"가 다음 질문이었습니다. **안 합니다.**

- 구글 로그인은 Playwright·Selenium 을 탐지해 막습니다. 네이버 HMAC 과 같은 종류의 통제입니다.
- 더 중요한 건 계정입니다. Flow 를 돌릴 구글 계정이 **Gemini 키·GA4·애드센스를 쓰는 그 계정**
  입니다. 크레딧을 긁다 정지되면 백오피스 전체가 같이 죽습니다.
- 크레딧 미터링을 우회하는 행위 자체가 그 미터링의 목적입니다.

그래서 자동화를 **경계 안쪽에만** 붙였습니다. 손으로 하는 건 "Flow 에서 생성·다운로드"뿐이고,
그 뒤는 전부 자동입니다.

### 다중 클립 조립 (`clips`)

기존 `segments` 는 **한 원본 안에서** 여러 구간을 뜨는 것이라 재활용할 수 없었습니다. 출처가
다른 파일들을 잇는 건 다른 문제입니다.

| | `segments` | `clips` (신규) |
|---|---|---|
| 입력 | 한 파일을 여러 번 열기 | 서로 다른 파일 N개 |
| 해상도 | 같은 원본이라 자동으로 일치 | 제각각 → **정규화 필요** |
| 오디오 | 원본 트랙을 이어 붙임 | 구성이 달라 못 붙임 → 내레이션만 |

concat 은 입력 구성이 같아야 붙습니다. 클립마다
`scale=…:force_original_aspect_ratio=decrease,pad=…,setsar=1,fps=30` 로 맞춘 뒤 이었습니다.
비율을 유지하고 남는 자리는 검게 채웁니다 — 잘라내면 캐릭터가 프레임 밖으로 나갑니다.

오디오는 내레이션 하나만 씁니다. 클립마다 오디오 유무와 채널 구성이 달라 그대로 concat 하면
통째로 실패합니다. `apad` 로 무한 무음을 이어 붙이고 `-shortest` 로 끊으면 내레이션이 짧아도
길어도 **영상 길이가 결과를 정합니다.**

### 자막은 클립 경계에 맞춘다

`autoCaptions` 는 전체 길이를 줄 수로 균등 분할합니다. 클립 길이가 서로 다르면 자막이 장면
경계를 넘어갑니다. 장면마다 대사가 정해진 구조에서는 어긋난 티가 바로 납니다. `clipCaptions`
가 클립 실제 길이를 받아 경계에 맞춥니다 — **계획 초가 아니라 파일 길이를 믿습니다.**
Flow 가 정확히 요청한 초를 주지 않기 때문입니다.

### buildFilter 를 쪼갰다

단일 원본은 `scale → pad → 글자` 한 체인이면 되지만, 조립은 `클립별 정규화 → concat → pad
→ 글자` 입니다. 글자 부분이 같아서 `geometry()` 와 `buildTextChain()` 으로 떼어내 양쪽이
공유합니다. 각자 그리면 한쪽에만 손댔을 때 자막 위치가 조용히 어긋납니다.

떼어내면서 주석 하나를 바로잡았습니다. `enable='between(t\,1\,2)'` 의 `\,` 는 **JS 템플릿
리터럴에서 백슬래시가 탈락**해 실제로는 그냥 `,` 입니다. 콤마가 필터 구분자와 충돌하지 않는
이유는 이스케이프가 아니라 **작은따옴표로 감싸여 있어서**였습니다. 동작은 정상이지만 주석이
없는 방어를 설명하고 있었습니다.

### 워커는 손대지 않았다

`parseRenderOptions` + `renderShort` 하나로 웹과 워커가 공유하는 구조라, 새 필드를 파서에
넣으니 워커는 그대로 동작합니다. 예전에 "필드를 여기서 다시 세면 어긋난다"고 적어둔 게
그대로 값을 했습니다.

### 업로드 정렬

한 번에 여러 개를 받으면서 정렬을 `Intl.Collator(numeric)` 로 했습니다. 사전순이면
`scene-10` 이 `scene-2` **앞**에 옵니다. 실제로 그 순서로 올려 3개가 1·2·10 으로 정렬되는지
확인했습니다.

## 15. 두 채널을 같은 조작으로 맞췄다

쇼츠는 역사 소재로 시작했다가 게임으로 방향을 바꿨다. 그러면서 화면이 옛 전제를 그대로
들고 있었다.

**걷어낸 것**

- `내 소재` 업로드 카드 — 공공누리 출처·라이선스 입력이 붙어 있었다. e영상역사관 대한뉴스를
  받아 넣기 위한 것이라 게임에는 쓸 데가 없다.
- Internet Archive 검색과 파일 목록 — 기록영상 소재용이다.
- `탭 두 개`(지금 뜨는 것 / 가공 가능한 소재) — 게임에서는 소재를 남한테서 구하지 않으니
  구분이 의미가 없어졌다.
- 렌더의 `원본 주소` 에 archive.org 직링크를 붙여넣던 흐름. **영상 링크를 직접 받으면 그
  영상이 써도 되는 것인지 판별이 다시 들어간다.** 그 판별을 없애려고 링크 입력 자체를
  걷어내고, 내가 녹화한 파일 경로만 받는다.

API 쪽 `mode=sources` / `mode=files` 는 남겨뒀다. 역사 채널로 되돌릴 때 화면만 다시
붙이면 된다.

**맞춘 것 — 표에서 행을 누르면 다음 단계로 간다**

키워드 탐색 화면이 이미 그 형태였다. 검색 → 표 → 행에서 `작성`. 쇼츠와 유아 채널도 같게
했다.

| 화면 | 행을 누르면 |
|---|---|
| 키워드 탐색 | 그 키워드로 글 작성 |
| 쇼츠 | 그 영상의 댓글 타임스탬프를 집계해 하이라이트 구간 |
| 유아 채널 | 그 영상의 구성을 분석해 기획안까지 한 번에 |

유아 채널은 체크박스로 여러 개 고르고 주제를 따로 입력하던 것을 바꿨다. 행을 누르면 제목·
설명란·길이를 Claude 에 넘겨 **왜 그 영상이 먹혔는지 구성을 먼저 분석하게** 하고(`analysis`),
그 구조로 새 캐릭터·새 소재의 기획을 만든다. 주제도 모델이 정하므로 입력이 선택이 됐다.

제목만 넘기던 것과 다른 점은 설명란이다. 유아 영상 설명란에는 수록곡 목록과 타임스탬프가
들어 있어서, 몇 개 항목을 어떤 순서로 다루고 후렴을 어디서 반복하는지가 읽힌다.

분석 대상 영상은 화면이 되돌려 보낸 값을 쓰지 않고 서버에서 `videoDetails` 로 다시 읽는다.
사용자가 고친 값이 섞이면 분석 근거가 흔들린다.

**Flow 왕복을 줄였다**

Flow 는 한 번에 한 클립씩 만든다. 전체 프롬프트를 한 덩어리로 복사하는 버튼만 있으면
장면마다 그 안에서 해당 부분을 찾아 잘라 써야 한다. 장면 행마다 `복사` 를 붙여 한 장면씩
바로 붙여넣게 했다. 캐릭터 시트와 화풍은 복사 시점에 자동으로 앞에 붙는다.

그래서 유아 채널 화면이 Flow 왕복 그대로가 됐다 — 3단계에서 장면 프롬프트를 복사해 Flow 에
넣고, 만들어 받은 클립들을 4단계에 한 번에 올리면 자막·내레이션까지 얹어 한 편으로 합친다.

## 16. 현재 상태

- 키워드 탐색(검색량·모바일 비중·광고 흡수율) → 경쟁 분석(상위권 신선도·인플루언서 점유율)
  → 글 작성 → 미리보기 → 네이버/티스토리 복사 → 글 목록 관리: **동작함**.
  실제 검색광고 키와 실제 검색 페이지로 조회까지 확인
- 문서수·경쟁률·추세: **막힘** (8장). 코드 경로와 UI 열은 그대로 두어, 승인된 키가 생기면
  설정 화면에 넣는 것만으로 채워집니다
- 크리에이터 어드바이저 연동은 제거. 내 블로그 유입 통계가 필요하면 어드바이저 UI에서 직접
  확인하는 편이 낫습니다.
- 자동 발행은 여전히 없습니다 (네이버 글쓰기 API 폐지, 티스토리 Open API 종료).
