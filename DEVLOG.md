# 개발 로그

이 백오피스를 만들면서 내린 결정과, 도중에 발견한 것들을 정리한 문서입니다.

> 이 저장소는 공개 저장소입니다. 작업 중 실제 네이버 세션 쿠키로 테스트했지만,
> 쿠키·API 키는 이 문서를 포함해 저장소 어디에도 기록하지 않았습니다.
> 인증 정보는 `.gitignore`된 로컬 `data/backoffice.db`에만 저장됩니다.

## 1. 요구사항

크리에이터 어드바이저에서 네이버 트렌드·주제별 인기 키워드를 확인하고, **메인 키워드 + 서브
키워드**로 제목을 잡아 글을 쓴 뒤, 같은 글을 티스토리에도 올리는 백오피스.

## 2. 초기 결정

| 항목 | 선택 | 이유 |
|---|---|---|
| 데이터 수집 | 쿠키 + 내부 API 직접 호출 | Playwright 자동 로그인은 캡차/2FA에 막히고 느림 |
| 발행 | 복사용 결과물만 생성 | 네이버 글쓰기 API 폐지, 티스토리 Open API 종료 |
| 본문 생성 | Gemini | 사용자 보유 키 |
| 형태 | Next.js 로컬 웹앱 | GUI로 키워드 조회·편집·미리보기 관리 |

의존성은 Next/React뿐입니다. SQLite는 Node 22.5+ 내장 `node:sqlite`를 써서 네이티브 빌드가
없습니다.

## 3. 깨지기 쉬운 부분에 대한 설계

> 3~5장은 크리에이터 어드바이저를 쓰려던 시절의 기록입니다. 여기서 언급하는 `naver.ts`와
> `curl.ts`는 6장에서 공식 API로 갈아타며 삭제했습니다. 왜 갈아탔는지의 근거라 남겨둡니다.

크리에이터 어드바이저는 공개 API가 아니라 경로와 응답 스키마가 예고 없이 바뀝니다. 그래서 둘 다
코드에 고정하지 않았습니다.

- **경로** — DB에 저장된 편집 가능한 프리셋. UI에서 수정하거나, DevTools `Copy as cURL`을
  붙여넣으면 [`src/lib/curl.ts`](src/lib/curl.ts)가 URL을 파싱해 바로 조회
- **응답 스키마** — [`src/lib/naver.ts`](src/lib/naver.ts)의 파서가 JSON을 재귀로 훑어
  "키워드처럼 생긴 문자열 필드를 가진 객체 배열" 중 가장 큰 것을 선택. `keyword` /
  `queryKeyword` / `query` 등 필드명 변형과 중첩 구조를 흡수하고, 실패하면 원본 JSON을 UI에 노출

## 4. 검증

- `npm run build` 통과, 서버 기동·DB 생성·API 응답 확인
- 순수 로직 29개 검증: 응답 스키마 4종 파싱, cURL 셸 인용 규칙(작은/큰따옴표, 백슬래시
  continuation, `-X` 플래그 스킵), 마크다운 변환, XSS 이스케이프

변환기 버그 하나를 잡았습니다. 마크다운 `##`이 `<h3>`로 나가 제목(h1) 다음 h2를 건너뛰었고,
`#`만 h2로 끌어올리고 나머지는 유지하도록 [수정](src/lib/markdown.ts)했습니다.

## 5. 실제 호출에서 막힌 지점 — 요청 서명

실제 세션 쿠키로 테스트하자 전 엔드포인트가 `403 {"status":"fail","message":"Forbidden"}`을
반환했습니다. 로그인 리다이렉트 HTML이 아니라 API 핸들러가 낸 JSON이라 경로 문제가 아니었습니다.

프론트엔드 번들(`/assets/index-*.js`)을 확인해 실제 구조를 파악했습니다.

모든 호출은 `` `/api/v6${path}` ``로 조립되고, 실제 경로는 이렇습니다.

```
/api/v6/accounts/channels
/api/v6/home/popular-category-keyword   ?service=&channelId=&date=
/api/v6/home/popular-demo-keyword       ?service=&channelId=&date=
/api/v6/home/weekly-recommendation      ?service=&date=
/api/v6/home/soaring-contents           ?service=&channelId=&interval=&date=
/api/v6/trend/query-demo-distribution   ?interval=&date=&keyword=
/api/v6/inflow-analysis/referrer-query-rank ?service=&channelId=&metric=&interval=&date=&ct=&limit=
/api/v6/dashboard/channel-ranks         ?service=&channelId=
```

`service`는 `naver_blog`, `channelId`는 블로그 아이디입니다.

문제는 경로가 아니었습니다. 모든 요청에 **HMAC 서명 헤더**가 붙습니다.

```
X-CA-Nonce   crypto.randomUUID()
X-CA-Ts      Date.now()
X-CA-Sig     HMAC("METHOD|...|ts|nonce", <__ca_key 쿠키에서 파생한 키>)
```

응답이 `403` + `code: "CA_KEY_INVALID"`면 `/api/v6/accounts/channels`를 다시 호출해 CA 키를
재발급받고 원 요청을 1회 재시도합니다. 그래서 **`/accounts/*`만 서명 없이 통과**하고(키 발급
엔드포인트라서), 나머지는 전부 403이 됩니다. 실제로 `/accounts/channels`는 정상 응답했습니다.

이건 스크립트 접근을 막으려고 네이버가 의도적으로 넣은 통제입니다. 서명 알고리즘을 복제해
우회하는 방향은 택하지 않았습니다.

## 6. 공식 API로 교체

쿠키 경로를 걷어내고 공식 API 세 곳으로 갈아탔습니다. 쿠키 만료도, 무단 우회도 없습니다.

| API | 얻는 것 | 인증 | 한도 |
|---|---|---|---|
| 검색광고 · 키워드도구 | 연관 키워드 + 월간 검색수(PC/모바일) + 광고 경쟁정도 | HMAC 서명 (발급받은 비밀키) | 계정별 |
| 검색 API · 블로그 | 키워드별 총 문서수 | Client ID/Secret | 일 25,000 |
| 데이터랩 · 검색어트렌드 | 검색 추이 (상대지수) | Client ID/Secret | 일 1,000 |

### 세 소스를 합치는 이유

키워드도구의 검색수만으로는 "쓸 만한 키워드"를 못 고릅니다. 검색량이 많으면 그만큼 경쟁도
치열하기 때문입니다. 그래서 검색 API로 그 키워드의 블로그 문서수를 세서
**경쟁률 = 문서수 ÷ 월간 검색수** 를 계산합니다. 낮을수록 수요 대비 공급이 적다는 뜻입니다.
공식 지표가 아니라 경험칙이라 UI에도 그렇게 적어뒀고, 절대값보다 같은 표 안의 상대 비교로
쓰도록 안내합니다.

데이터랩은 계절성·상승세를 보는 용도입니다. 다만 키워드 그룹을 5개까지만 받아서 전체 표에는
못 붙입니다. 상위 5개만 자동 조회하고, 나머지는 표에서 체크해 `선택 N개 추세 보기`로 따로
부르게 했습니다.

### 서명 (`src/lib/searchad.ts`)

5장에서 우회하지 않기로 한 그 HMAC이, 공식 API에서는 **발급받은 비밀키로 정당하게** 서명하는
방식으로 그대로 쓰입니다.

```
X-Timestamp  Date.now()
X-API-KEY    액세스라이선스
X-Customer   CUSTOMER_ID
X-Signature  base64(HMAC-SHA256(`${timestamp}.${METHOD}.${path}`, 비밀키))
```

`path`에 쿼리스트링을 넣으면 안 됩니다. `/keywordstool?hintKeywords=...` 가 아니라
`/keywordstool` 로 서명합니다.

### 응답에서 걸린 것

월간 검색수가 10 미만이면 숫자가 아니라 `"< 10"` 문자열로 옵니다. 그대로 `Number()` 하면
`NaN`이 되고 정렬이 무너져서, `parseCount()`가 9로 환산합니다. UI 각주에도 이 값이 실측이
아니라 마스킹된 값이라고 적어뒀습니다.

힌트 키워드는 최대 5개이고, 검색광고는 공백을 제거한 형태로 집계합니다. `normalizeHints()`가
공백 제거·중복 제거·5개 절단을 한 번에 합니다.

### 부분 실패 처리

검색광고가 실패하면 표 자체가 안 나오지만, 검색 API나 데이터랩이 실패해도 검색량 표는
그대로 나옵니다. 어디까지 성공했는지는 `SourceStatus[]` 로 내려서 결과 위에 배지로 붙입니다.
문서수 조회는 키워드 1건당 1호출이라 동시 실행을 4로 제한하고, 개별 실패는 그 키워드만
`null`로 남기고 넘어갑니다.

## 7. 검증

- `npm run build` 통과
- 자격증명 없는 상태에서 `/api/keywords`, `/api/trend`, `/api/settings/test` 가 모두
  500이 아니라 안내 문구를 반환하는지 확인
- 더미 자격증명으로 실제 엔드포인트를 호출해, 요청이 네이버 서버까지 도달하고 **인증
  사유로만** 거절되는지 확인 (`403 Invalid API-KEY`, `401 NID AUTH Result Invalid`).
  요청 형식·헤더·서명 경로 자체는 정상이라는 뜻입니다.

이 과정에서 403 안내 문구를 고쳤습니다. 키가 틀리면 401이 올 거라 예상하고 403은 "권한 없음"
으로 안내했는데, 실제로는 키가 틀려도 403 `Invalid API-KEY` 가 옵니다. 두 코드를 같은
안내로 합쳤습니다.

## 8. 검색 API·데이터랩이 신규 발급 중단

키를 발급받으려니 애플리케이션 등록 화면의 **사용 API 드롭다운에 `검색`과 `데이터랩`이
아예 없었습니다.** 남아 있는 건 네이버 로그인·인증서·전자문서·네이버페이·카페·캘린더·캡차뿐.

발급된 Client ID/Secret으로 그냥 호출해 봤더니 이렇게 갈립니다.

```
가짜 키   → 401 NID AUTH Result Invalid (1000)
실제 키   → 401 Scope Status Invalid
```

에러가 다릅니다. **키는 유효한데 앱에 그 API 스코프가 없다**는 뜻입니다. 신규 제휴를 받지
않는 게 맞았습니다. 이 두 케이스를 "Client ID/Secret 이 잘못됐습니다" 하나로 뭉뚱그리고
있어서, `errorDetail()`이 `scope` 문자열을 보고 갈라 안내하도록 고쳤습니다.

### 대안 — 검색광고 응답을 더 짜냈다

키워드도구는 `showDetail=1`이면 검색수 말고도 광고 지표를 줍니다. 이미 받아오면서 안 쓰던
필드로 두 지표를 만들었습니다.

| 지표 | 계산 | 뜻 |
|---|---|---|
| 모바일 비중 | 모바일 검색 ÷ 전체 검색 | 블로그 유입은 모바일이 지배적 |
| 광고 흡수율 | 월평균 광고 클릭수 ÷ 월간 검색수 | 검색 중 몇 %가 광고로 빠지는지 |

광고 흡수율이 문서수의 빈자리를 제일 잘 메웁니다. 광고주가 돈을 안 쓰는 키워드는 상업적
의도가 낮은 정보성 검색이고, 그런 검색일수록 사용자가 블로그 본문을 읽습니다. 문서수처럼
공급을 직접 세는 건 아니지만, 수요의 **성격**을 알려줍니다.

실제로 `제주도 여행` 시드에서 갈린 결과:

```
7월제주도날씨   31,960   모바일 94.5%   광고 흡수 0%
부산숙소       108,300   모바일 80.1%   광고 흡수 2.4%
```

검색량은 부산숙소가 3배지만 예약 의도라 광고가 가져갑니다.

### 정렬 순서 때문에 반쯤 헛돌던 것

새 정렬을 붙이고 결과를 찍어 보니 검색수 내림차순 그대로였습니다. 라우트의 허용 정렬키
화이트리스트에 새 키를 안 넣어서 `searches`로 폴백하고 있었습니다.

고치고 나서 더 큰 문제가 보였습니다. `limit`을 **검색수 기준으로 먼저 자른 뒤** 정렬해서,
`광고 흡수율 낮은 순`이 실제로는 "검색수 상위 N개를 흡수율로 재배열"한 것이었습니다.
948건 중 정작 흡수율 낮은 키워드는 후보에도 못 들었습니다. 자르기 전에 최종 기준으로 먼저
정렬하도록 바꿨더니 결과가 완전히 달라졌습니다 — 위의 `7월제주도날씨`가 그렇게 나온
키워드입니다.

문서수·경쟁률 정렬만은 예외로 남겼습니다. 그 값은 자른 **뒤에** 조회하는 거라 미리 정렬할
수가 없습니다.

## 9. 문서수 대신 SERP 를 본다

문서수를 살릴 수 있나 해서 검색 결과 페이지를 직접 읽어 봤습니다. 결론은 **차단이 아니라
숫자가 없다**였습니다.

```
HTTP 200, 642KB, blog.naver.com 링크 374개, title "제주도여행 : 네이버 블로그검색"
"약 N건" 표기 → 없음 (PC / 모바일 / where=blog / where=post / where=view 전부)
```

네이버가 총 건수 노출을 없앴습니다. `문서수 ÷ 검색량` 은 재료가 사라져 포기했습니다.

그 대신 페이지에서 확실히 나오는 두 가지로 방향을 틀었습니다. **"얼마나 많이 있나"(공급량)
대신 "지금 상위를 누가 얼마나 단단히 잡고 있나"** 를 봅니다.

| 지표 | 뜻 |
|---|---|
| 최신글 비율 | 상위 노출 글 중 최근 30일 내 비율. 높으면 계속 갈아치워지는 레드오션 |
| 인플루언서 비율 | 상위 노출 블로그 중 인플루언서 비율. 높으면 일반 블로그가 밀림 |

### 파서를 어디에 고정했나

결과 아이템 래퍼는 `m65iNUxtb_Effkaj` 같은 해시 클래스라 배포마다 바뀝니다. 대신 값이 든
디자인시스템 클래스 하나에만 기댔습니다.

```
sds-comps-profile-info-subtext  →  "2026.02.21." / "4주 전" / "6일 전"
```

42건이 잡음 없이 떨어졌고(절대 31 + 상대 11), 결과 수와도 일치했습니다.

인플루언서 판정에서 한 번 헛짚었습니다. `in.naver.com/{핸들}` 을 그대로 세니
`home`, `delivery`, `identity-access` 같은 네이버 내부 경로가 섞였습니다. **결과에 실제로
뜬 `blog.naver.com` 핸들과 교집합인 것만** 세도록 바꾸니 내부 경로가 자연히 걸러집니다.

### 변별력 확인

지표를 만들었으면 실제로 갈라지는지 봐야 합니다. 처음 4개로 재보니 난이도가 33~45로 몰려
있어서, 성격이 확연히 다른 키워드로 다시 쟀습니다.

```
8월제주날씨     57   최신글 90%   중앙연령 14일
맛집           52   최신글 100%  중앙연령 14일
가을축제        33   최신글 48%   중앙연령 43일
제주도여행       23   최신글 31%   중앙연령 53일
제주도박물관     17   최신글 20%   중앙연령 48일
대한민국구석구석   15   최신글 27%   중앙연령 43일
```

15~57 로 벌어졌고 해석도 맞습니다. 지금이 8월이라 `8월제주날씨` 에 새 글이 쏟아지는 중이고,
`맛집` 은 상위 100%가 최근 한 달입니다.

**두 지표가 엇갈리는 게 이 조합의 핵심입니다.** `8월제주날씨` 는 광고 흡수율 0% 라 광고
지표만 보면 좋은 키워드인데, 진입 난이도는 57 입니다. 광고 흡수율은 **수요의 성격**을,
진입 난이도는 **지금 공급 상태**를 봅니다. 한쪽만 봤으면 놓쳤을 판단입니다.

### 선은 지켰다

공식 API 가 아니라 공개 검색 페이지를 읽습니다. 다만 5장에서 거절한 서명 우회와는 다릅니다.
인증을 뚫는 게 아니라 로그인 없이 누구나 보는 페이지를 읽습니다. 그래도 크롤러는 아니게
두려고, **사용자가 버튼을 눌렀을 때만** 동시 2개·간격 0.4초·한 번에 20개 상한으로 돕니다.
마크업이 바뀌면 조용히 0건이 되지 않도록, 한 건도 못 읽으면 오류로 올립니다.

## 10. 애드센스를 달기로 하면서 지표가 뒤집혔다

9장까지의 전제는 "광고가 안 가져가는 키워드가 좋다"였습니다. 블로그 유입만 볼 때는 맞지만,
애드센스를 달면 **광고주가 돈을 쓰는 주제라야 단가가 붙습니다.** 흡수율이 0%인 키워드는
트래픽이 와도 수익이 거의 없습니다.

단가 데이터가 필요해졌는데, 검색광고 API 에 입찰가 추정 엔드포인트가 있었습니다.

```
POST /estimate/average-position-bid/keyword
  { device, keywordplus, key, items: [{key, position: 1}] }
→ { estimate: [{ keyword, position, bid }] }
```

키워드도구와 같은 서명 방식이라 그대로 붙었습니다. 블로그 유입이 모바일 위주라 `MOBILE`
기준으로 받습니다. 애드센스 CPC 그 자체는 아니지만, 같은 시장에서 같은 광고주가 그 주제에
얼마를 쓰는지를 보여주는 상관 지표입니다.

여기에 트래픽을 곱해 `수익 잠재력 = 검색수 × 단가 ÷ 1000` 을 만들고 기본 정렬로 뒀습니다.
단가만 높고 아무도 안 찾는 키워드와, 검색량만 많고 단가가 바닥인 키워드를 동시에 거릅니다.

### 분야별로 실제로 재봤다

만든 도구로 후보 분야를 직접 측정했습니다.

| 분야 | 평균 입찰가 | 평균 진입난이도 |
|---|---|---|
| 보험/법률 | 39,894원 | 21 |
| 건강 | 8,085원 | 24 |
| 재테크 | 5,038원 | 20 |
| 생활정보 | 4,711원 | 30 |
| 테크/IT | 2,273원 | 26 |

통념과 어긋나는 결과가 나왔습니다. **보험/법률은 단가가 테크의 17배인데 진입 난이도는 오히려
가장 낮은 축**입니다. 고단가면 경쟁도 심할 거라 예상했는데 반대였습니다.

이유는 광고 경쟁과 블로그 경쟁이 다른 시장이기 때문입니다. 보험사는 파워링크에 돈을 쓰지
블로그 글로 싸우지 않습니다. `교통사고합의금` 은 인플루언서 0%, 중앙연령 56일 — 오래된 글이
그대로 상위에 남아 있습니다.

`뉴스/시사` 는 후보에서 빠졌습니다. `속보` 는 연관 키워드가 0건이고, `시사상식` 은 언론사명
(한국경제·매일경제)으로, `부동산 뉴스` 는 법률 용어(낙찰·주택법)로 흩어집니다. 뉴스 수요는
"지금 그 사건"에 붙는데 그건 월간 집계에 잡히지 않고 며칠이면 사라집니다.

### 호출 한도

분야를 연속으로 조회하다 `HTTP 429 Too Many Requests` 를 맞았습니다. 검색광고 API 는 짧은
간격의 연속 호출을 막습니다. 오류 문구로 그대로 노출되니 잠시 뒤 다시 조회하면 됩니다.
입찰가는 50개씩 끊어 부르고, 후보 풀을 100개로 제한해 조회 1회당 최대 3호출로 묶었습니다.

## 11. 시각 자료를 이미지 대신 HTML 로

글에 시각 자료를 넣기로 했는데, 이미지 생성은 택하지 않았습니다.

| | 이미지 | HTML |
|---|---|---|
| 만들기 | 별도 API·저장소·업로드 | 본문과 같은 응답에 담김 |
| 옮기기 | 붙여넣기로 안 됨 | 티스토리 HTML 모드에 그대로 |
| 검색엔진 | 글자를 못 읽음 | **내용을 읽음** |

Gemini 가 `visuals` 배열로 비교표·절차·체크리스트를 만들고, 본문 마크다운의 `{{visual:N}}`
자리에 끼워 넣습니다. 네이버 스마트에디터는 태그를 대부분 지우므로 사실상 티스토리 전용입니다.

### 정제기가 필요했다

모델이 만든 HTML 을 그대로 렌더하면 **미리보기 화면에서 스크립트가 실제로 돕니다.** 통과시킬
태그와 CSS 속성을 화이트리스트로 못박고 나머지는 텍스트로 떨어뜨립니다.

테스트가 버그를 하나 잡았습니다. `<script>` 태그는 지우는데 **안쪽 내용이 텍스트로
살아남아** `alert(1)` 같은 소스가 본문에 노출됐습니다. 태그만 버리고 내용을 남기는 규칙이
스크립트류에는 정반대로 작동한 것입니다. `script`·`style`·`iframe` 은 닫는 태그까지 통째로
건너뛰도록 고쳤습니다. 26개 단언 통과.

같은 시기에 표 렌더링 버그도 잡았습니다. SEO 프롬프트가 표를 최소 1개 요구하는데
`markdownToHtml` 이 파이프 문법을 몰라서, **생성되는 모든 글에서 표가 `<p>| 구분 | 한도 |`
로 뭉개지고 있었습니다.** 정렬 표기(`:---:`)까지 지원하도록 고쳤고, 그 과정에서 대시 1개짜리
구분줄(`:-:`)을 못 읽는 버그도 함께 잡았습니다.

## 12. GA4·애드센스 연동 — 구글의 벽 세 개

성과를 회수하지 못하면 키워드 선택이 맞았는지 알 수 없습니다. 루프를 닫으러 갔다가 벽을
셋 만났습니다.

**① 애드센스는 서비스 계정을 안 받는다.** GA4 Data API 는 서비스 계정 JWT 로 되는데,
AdSense Management API 는 OAuth2 사용자 동의만 받습니다. 그래서 두 연동의 인증 구조가
아예 다릅니다.

**② gcloud 로는 애널리틱스 권한을 못 받는다.** 서비스 계정을 GA4 속성에 등록하는 걸
자동화하려고 `gcloud auth application-default login --scopes=...analytics.edit` 을 실행했더니
**"차단된 앱"** 이 떴습니다. gcloud 의 내장 OAuth 클라이언트는 검증되지 않아 민감 범위를
요청할 수 없습니다.

**③ 쿠키를 복사해도 세션이 안 따라온다.** 브라우저 자동화를 붙이려고 Chrome 프로필의
쿠키 DB 를 복사했지만 로그인이 유지되지 않았습니다. Chrome 127+ 의 **앱 바인딩
암호화(App-Bound Encryption)** 가 프로필 도난을 막습니다. Chrome 151 은 기본 프로필에서
`--remote-debugging-port` 자체도 무시합니다.

셋 다 우회 대상이 아니라고 판단했습니다. 5장에서 네이버 HMAC 서명을 복제하지 않기로 한
것과 같은 성격입니다.

### 그래서 어디까지 자동화됐나

우회 대신 **정식 경로를 자동화**했습니다.

| 단계 | 방법 |
|---|---|
| API 3종 활성화·서비스 계정·키 발급 | `gcloud` CLI |
| GA4 속성·데이터 스트림 생성 | Analytics **Admin API** |
| 서비스 계정 권한 부여 | 브라우저 자동화 (사용자가 로그인 1회) |
| 티스토리 GA 태그 설치 | 브라우저 자동화 |

속성 생성은 처음에 GA4 관리 화면을 클릭으로 몰려다 실패했습니다. 오버레이가 클릭을
가로채고 직접 URL 은 홈으로 튕겼습니다. **서비스 계정에 계정 수준 편집자 권한을 준 뒤
Admin API 로 만드는 쪽으로 바꾸니 한 번에 끝났습니다.** SPA 를 클릭으로 모는 것보다 API 가
언제나 낫습니다.

태그 설치는 실제 블로그 페이지를 열어 `googletagmanager.com/gtag/js?id=...` 요청이 실제로
나가는지까지 확인했습니다. 설정 화면에서 "사용중"으로 보이는 것과 태그가 실제로 발화하는
것은 다릅니다.

### 0 과 오류를 구분한다

태그를 막 달아서 조회수가 0 입니다. 이때 화면에 그냥 0 만 뜨면 고장난 것처럼 보입니다.
연결 테스트가 두 상태를 갈라 안내합니다.

```
연결은 정상입니다. 다만 수집된 데이터가 아직 없습니다 (태그 설치 직후면 몇 시간 걸립니다).
```

## 13. 영상 생성 도구 세 곳을 확인했다 — "무료"의 실체

유아 채널 영상을 무료로 만들 수 있다는 얘기를 듣고 세 곳을 확인했습니다. 결과가 갈렸습니다.

| 도구 | 무료 API | 실제 상황 |
|---|---|---|
| 구글 플로우 | **API 자체가 없음** | Flow(flow.google)는 웹 UI 제품입니다. 같은 Veo 모델의 개발자 경로는 Gemini API 이고 **유료** — 초당 $0.15~0.40 |
| HeyGen | **2026년 2월 폐지** | 무료 API 티어가 없어졌습니다. 유료 종량제만 남았고, 말하는 아바타라 유아 애니메이션엔 애초에 안 맞습니다 |
| Fish Audio | **있음** | `s2.1-pro-free` 가 유료와 **같은 모델**입니다. 83개 언어, 하드 캡 없음(Fair Use 적용) |

"플로우로 무료"는 **웹 UI 이야기**였습니다. Google AI 플랜 크레딧으로 브라우저에서 만드는
것이고, 코드에서 부를 수 없습니다. 그래서 두 갈래로 나눴습니다.

- **무료 경로** — Flow 웹에서 만들어 내려받고, 쇼츠 화면의 소재 업로드로 넣습니다.
  기획안 화면에 `전체 프롬프트 복사 (Flow 용)` 버튼을 둬서 붙여넣기만 하면 되게 했습니다.
- **자동 경로** — Gemini API 의 Veo. 유료라는 걸 화면에 금액과 함께 박아뒀습니다
  (8초 클립 $1.2~3.2, 8장면이면 한 편에 $10~26).

### Veo 는 Gemini 키를 그대로 쓴다

Veo 도 같은 `x-goog-api-key` 라 **이미 있는 다중 키 로테이션이 그대로 재활용**됩니다.
새 자격증명이 하나도 늘지 않았습니다. 다만 Veo 는 무료 티어에 없어서 결제가 설정된
프로젝트의 키여야 하고, 429·403 에 그 안내를 실었습니다.

기획안의 장면 초와 Veo 가 받는 초가 다릅니다. Veo 3.1 은 4·6·8초만 받아서, 기획안 값을
가장 가까운 허용값으로 맞춰 보냅니다(5초→4, 7초→6, 15초→8).

### 결과 영상은 프록시를 거친다

Veo 가 주는 URI 는 API 키 헤더가 있어야 열립니다. 브라우저는 헤더를 붙일 수 없어서 서버가
대신 받아 흘려보냅니다. 이때 **URI 를 그대로 믿으면 임의 주소로 키를 보내는 통로가 됩니다.**
호스트가 `googleapis.com` 인 것만 통과시킵니다 — `notgoogleapis.com` 같은 유사 도메인이
정규식에 걸리지 않게 `(^|\.)googleapis\.com$` 로 고정했습니다.

### Fish Audio 의 단서

무료 등급은 "일부 상용 시나리오에 제약이 있을 수 있다"고 공지돼 있습니다. 수익화 채널에
쓸 거라면 유료 등급이 안전해서, 모델을 고정하지 않고 설정에서 바꾸게 뒀습니다.

## 14. 손으로 만든 클립을 자동으로 조립한다

Flow 는 API 가 없으니 "그럼 웹 자동화냐"가 다음 질문이었습니다. **안 합니다.**

- 구글 로그인은 Playwright·Selenium 을 탐지해 막습니다. 네이버 HMAC 과 같은 종류의 통제입니다.
- 더 중요한 건 계정입니다. Flow 를 돌릴 구글 계정이 **Gemini 키·GA4·애드센스를 쓰는 그 계정**
  입니다. 크레딧을 긁다 정지되면 백오피스 전체가 같이 죽습니다.
- 크레딧 미터링을 우회하는 행위 자체가 그 미터링의 목적입니다.

그래서 자동화를 **경계 안쪽에만** 붙였습니다. 손으로 하는 건 "Flow 에서 생성·다운로드"뿐이고,
그 뒤는 전부 자동입니다.

### 다중 클립 조립 (`clips`)

기존 `segments` 는 **한 원본 안에서** 여러 구간을 뜨는 것이라 재활용할 수 없었습니다. 출처가
다른 파일들을 잇는 건 다른 문제입니다.

| | `segments` | `clips` (신규) |
|---|---|---|
| 입력 | 한 파일을 여러 번 열기 | 서로 다른 파일 N개 |
| 해상도 | 같은 원본이라 자동으로 일치 | 제각각 → **정규화 필요** |
| 오디오 | 원본 트랙을 이어 붙임 | 구성이 달라 못 붙임 → 내레이션만 |

concat 은 입력 구성이 같아야 붙습니다. 클립마다
`scale=…:force_original_aspect_ratio=decrease,pad=…,setsar=1,fps=30` 로 맞춘 뒤 이었습니다.
비율을 유지하고 남는 자리는 검게 채웁니다 — 잘라내면 캐릭터가 프레임 밖으로 나갑니다.

오디오는 내레이션 하나만 씁니다. 클립마다 오디오 유무와 채널 구성이 달라 그대로 concat 하면
통째로 실패합니다. `apad` 로 무한 무음을 이어 붙이고 `-shortest` 로 끊으면 내레이션이 짧아도
길어도 **영상 길이가 결과를 정합니다.**

### 자막은 클립 경계에 맞춘다

`autoCaptions` 는 전체 길이를 줄 수로 균등 분할합니다. 클립 길이가 서로 다르면 자막이 장면
경계를 넘어갑니다. 장면마다 대사가 정해진 구조에서는 어긋난 티가 바로 납니다. `clipCaptions`
가 클립 실제 길이를 받아 경계에 맞춥니다 — **계획 초가 아니라 파일 길이를 믿습니다.**
Flow 가 정확히 요청한 초를 주지 않기 때문입니다.

### buildFilter 를 쪼갰다

단일 원본은 `scale → pad → 글자` 한 체인이면 되지만, 조립은 `클립별 정규화 → concat → pad
→ 글자` 입니다. 글자 부분이 같아서 `geometry()` 와 `buildTextChain()` 으로 떼어내 양쪽이
공유합니다. 각자 그리면 한쪽에만 손댔을 때 자막 위치가 조용히 어긋납니다.

떼어내면서 주석 하나를 바로잡았습니다. `enable='between(t\,1\,2)'` 의 `\,` 는 **JS 템플릿
리터럴에서 백슬래시가 탈락**해 실제로는 그냥 `,` 입니다. 콤마가 필터 구분자와 충돌하지 않는
이유는 이스케이프가 아니라 **작은따옴표로 감싸여 있어서**였습니다. 동작은 정상이지만 주석이
없는 방어를 설명하고 있었습니다.

### 워커는 손대지 않았다

`parseRenderOptions` + `renderShort` 하나로 웹과 워커가 공유하는 구조라, 새 필드를 파서에
넣으니 워커는 그대로 동작합니다. 예전에 "필드를 여기서 다시 세면 어긋난다"고 적어둔 게
그대로 값을 했습니다.

### 업로드 정렬

한 번에 여러 개를 받으면서 정렬을 `Intl.Collator(numeric)` 로 했습니다. 사전순이면
`scene-10` 이 `scene-2` **앞**에 옵니다. 실제로 그 순서로 올려 3개가 1·2·10 으로 정렬되는지
확인했습니다.

## 15. 두 채널을 같은 조작으로 맞췄다

쇼츠는 역사 소재로 시작했다가 게임으로 방향을 바꿨다. 그러면서 화면이 옛 전제를 그대로
들고 있었다.

**걷어낸 것**

- `내 소재` 업로드 카드 — 공공누리 출처·라이선스 입력이 붙어 있었다. e영상역사관 대한뉴스를
  받아 넣기 위한 것이라 게임에는 쓸 데가 없다.
- Internet Archive 검색과 파일 목록 — 기록영상 소재용이다.
- `탭 두 개`(지금 뜨는 것 / 가공 가능한 소재) — 게임에서는 소재를 남한테서 구하지 않으니
  구분이 의미가 없어졌다.
- 렌더의 `원본 주소` 에 archive.org 직링크를 붙여넣던 흐름. **영상 링크를 직접 받으면 그
  영상이 써도 되는 것인지 판별이 다시 들어간다.** 그 판별을 없애려고 링크 입력 자체를
  걷어내고, 내가 녹화한 파일 경로만 받는다.

API 쪽 `mode=sources` / `mode=files` 는 남겨뒀다. 역사 채널로 되돌릴 때 화면만 다시
붙이면 된다.

**맞춘 것 — 표에서 행을 누르면 다음 단계로 간다**

키워드 탐색 화면이 이미 그 형태였다. 검색 → 표 → 행에서 `작성`. 쇼츠와 유아 채널도 같게
했다.

| 화면 | 행을 누르면 |
|---|---|
| 키워드 탐색 | 그 키워드로 글 작성 |
| 쇼츠 | 그 영상의 댓글 타임스탬프를 집계해 하이라이트 구간 |
| 유아 채널 | 그 영상의 구성을 분석해 기획안까지 한 번에 |

유아 채널은 체크박스로 여러 개 고르고 주제를 따로 입력하던 것을 바꿨다. 행을 누르면 제목·
설명란·길이를 Claude 에 넘겨 **왜 그 영상이 먹혔는지 구성을 먼저 분석하게** 하고(`analysis`),
그 구조로 새 캐릭터·새 소재의 기획을 만든다. 주제도 모델이 정하므로 입력이 선택이 됐다.

제목만 넘기던 것과 다른 점은 설명란이다. 유아 영상 설명란에는 수록곡 목록과 타임스탬프가
들어 있어서, 몇 개 항목을 어떤 순서로 다루고 후렴을 어디서 반복하는지가 읽힌다.

분석 대상 영상은 화면이 되돌려 보낸 값을 쓰지 않고 서버에서 `videoDetails` 로 다시 읽는다.
사용자가 고친 값이 섞이면 분석 근거가 흔들린다.

**Flow 왕복을 줄였다**

Flow 는 한 번에 한 클립씩 만든다. 전체 프롬프트를 한 덩어리로 복사하는 버튼만 있으면
장면마다 그 안에서 해당 부분을 찾아 잘라 써야 한다. 장면 행마다 `복사` 를 붙여 한 장면씩
바로 붙여넣게 했다. 캐릭터 시트와 화풍은 복사 시점에 자동으로 앞에 붙는다.

그래서 유아 채널 화면이 Flow 왕복 그대로가 됐다 — 3단계에서 장면 프롬프트를 복사해 Flow 에
넣고, 만들어 받은 클립들을 4단계에 한 번에 올리면 자막·내레이션까지 얹어 한 편으로 합친다.

## 16. 현재 상태

- 키워드 탐색(검색량·모바일 비중·광고 흡수율) → 경쟁 분석(상위권 신선도·인플루언서 점유율)
  → 글 작성 → 미리보기 → 티스토리 복사 → 글 목록 관리: **동작함**.
  실제 검색광고 키와 실제 검색 페이지로 조회까지 확인
- 문서수·경쟁률·추세: **막힘** (8장). 코드 경로와 UI 열은 그대로 두어, 승인된 키가 생기면
  설정 화면에 넣는 것만으로 채워집니다
- 키워드 기반 글은 티스토리뿐이다. 한때 네이버에도 키워드 채널을 뒀다가 하루 만에
  걷어냈다 — 방문 안 한 가게 후기를 지어내지 않는다는 원칙과 부딪혔다. 18장 끝
  "채널의 뜻을 바로잡는다" 참고
- 티스토리 기존 초안 85편은 삭제했다(백업은 저장소가 아니라 사용자 데스크톱). 2주 동안은
  티스토리 시드로 키워드만 매일 모으고, 그동안 매일 블로그 초안 워크플로는 꺼 둔다. 18장
  참고. 첫 수집에서 시드 12개 → 키워드 365개(긴 키워드 108개)가 모였고, 광고 흡수율 0%인
  "계산기" 검색어 무리가 벌써 보인다
- 키워드 기준을 한 번 더 고쳤다 — 흡수율만 낮은 순으로 보면 광고주 자체가 없는 키워드가
  위로 온다. 지금은 **흡수율 2% 미만이면서 단가 2,000원 이상**인 것만 `검색량 × 단가`
  순으로 본다. 다음 모바일 통합검색으로 초기 후보 14개 중 7개가 실제로 경쟁 가능함을
  확인했다. 19장 참고
- GA4 옆에 **서치콘솔**을 붙였다(같은 서비스 계정, GA4 편집자 · 서치콘솔 전체 권한). 매일
  09:30 KST 깃액션(`insights.yml`)이 최근 7일 검색 노출·유입·이벤트를 덮어 쌓고, 성과
  화면에서 기회 검색어(노출은 되는데 5~30위)를 본다. 다음 글감은 **글감 큐 → 기회
  검색어 → 키워드 풀** 순으로 고른다. 초기 큐 7개가 들어가 있고, 발행은 여전히 사람이
  한다. 첫 90일 GA4 세션은 다이렉트 105 · 리퍼러 17, 서치콘솔에는 이미 노출이 몇 건
  잡혀 있다("건강보험료 확인방법" 47위). 19장 참고
- 계산기 글(3.3% 환급 계산기·세금계산기) 두 개를 추가했다 — 계산을 누르면 GA4
  `calculator_use` 이벤트(`calc_type`)를 쏜다. GA4 향상된 측정은 이미 전부 켜져 있었고,
  주요 이벤트·맞춤 측정기준만 Admin API로 멱등하게 추가했다(`scripts/ga4-setup.mjs`).
  19장 참고
- 쓰레드 제휴 후보 워크플로도 꺼 둔다 — 방향이 바뀌어 나중에 인스타그램과 같이 다시
  설계한다. 코드와 화면(`/threads`)은 남겨 둔다
- 네이버 방문 후기(사진 분석 → 카카오 로컬 보강 → 인터뷰 → 초안): 사진 분석·본문 생성을
  Gemini 에서 OpenAI(GPT, 기본 `gpt-5.5`)로 바꿨다 — 사진 인식이 핵심인 갈래라 시각 인식이
  강한 쪽에 맡겼다(18장). OpenAI 경로는 합성 메뉴판 사진으로 끝까지 확인했다 — 분석 5초에
  메뉴 6개·가격 전부 정확, 사진 번호 일치, 영수증 없음은 null, 본문 25초에 인터뷰에 없는
  사실을 지어내지 않았다. 카카오 키는 아직 설정에 없어 장소 보강만 못 돌려봤다.
- 개발 로그(수집 → 초안 → 승인 → 발행 → 역동기화): 코드 경로는 갖췄고 크론 하나가 네
  단계로 나뉘어 돈다. GitHub PAT·velog 쿠키로 수집·동기화까지는 확인했고, 발행(writePost)만
  첫 승인 때 실제로 본다.
- 영상 갈래(게임 쇼츠·유아 채널)는 걷어냈다. 17장 참고.
- 크리에이터 어드바이저 연동은 제거. 내 블로그 유입 통계가 필요하면 어드바이저 UI에서 직접
  확인하는 편이 낫습니다.
- 자동 발행은 여전히 없습니다 (네이버 글쓰기 API 폐지, 티스토리 Open API 종료).

## 17. 하나로 합친다 — 유튜브를 빼고 devlog 를 들이고 아이폰 설정 디자인으로

세 가지를 같은 시기에 정리했다. 서로 다른 결정이지만 방향은 같다 — **안 쓰는 걸 걷어내고,
쓰는 걸 한곳에 모은다.**

### 영상 갈래를 걷어냈다

게임 쇼츠·유아 채널은 ffmpeg 워커 하나와 YouTube·Claude·Veo·Fish Audio 네 개 API 를 끌고
다녔다. 블로그·쓰레드·velog 는 전부 글이라 같이 둘 이유가 없었고, 애초에 쓰지 않았다.
쓰지 않는 코드는 버그가 나도 아무도 모르고, `npm install` 만 해도 네 서비스의 SDK 가 같이
설치됐다. 유지 비용이 사용량보다 컸다.

`render_jobs` 테이블과 그 큐를 돌리던 `claim_render_job` 함수, 영상 갈래에서만 쓰던
자격증명(`youtube_api_key`, `anthropic_api_key`, `veo_model`, `fish_api_key` 등)은
[`20260925000000_drop_video.sql`](supabase/migrations/20260925000000_drop_video.sql) 이
지운다. 렌더 결과 파일은 애초에 로컬 `data/` 에만 있었으므로 DB 에서 잃는 건 작업 이력뿐이다.
`@anthropic-ai/sdk` 의존성도 같이 뺐다 — 유아 채널 기획에만 썼다.

### 왜 devlog 를 노션이 아니라 여기로

원래 [y10b/devlog](https://github.com/y10b/devlog) 는 별도 레포였다. 노션 DB 세 개(개발
로그·콘텐츠 캘린더·기술 스택)가 저장소였고, 초안을 검토·승인하는 화면도 그 노션 페이지였다.
문제는 승인이 **백오피스가 아니라 노션에서** 일어난다는 것이었다 — 블로그 글은 `/posts`,
쓰레드는 `/threads` 에서 검토하는데 velog 글만 다른 앱을 열어야 했다. 검토가 두 곳으로
갈리면 결국 한쪽을 덜 보게 된다.

옮기면서 얻은 건 검토 화면 통합만이 아니다. 자격증명도 한 곳에 모였다 — 전에는 GitHub
PAT·velog 쿠키를 GitHub Actions Secrets 에 직접 등록했지만, 이제는 다른 API 키들과 같이
설정 화면 DB 에 있다. 레포 시크릿은 `daily-post` 와 똑같이 `APP_ORIGIN`·`CRON_SECRET`
두 개면 된다 — 실제 자격증명은 배포본이 DB 에서 읽으니, 러너는 어떤 키가 있는지 몰라도 된다.

(뒤에 뒤집었다. 배포본 API 를 부르면 머지·배포·CRON_SECRET 이 먼저 있어야 첫 실행이 된다.
제휴 후보 워크플로처럼 러너에서 스크립트를 직접 돌리는 쪽으로 바꿨다 — DB 접속 시크릿만 있으면
이 파일이 있는 브랜치에서 바로 돈다. 크론 라우트는 남겨 두었지만 워크플로는 쓰지 않는다.)

### 노션 → Supabase 에서 달라진 것

| | 노션 | Supabase |
|---|---|---|
| 본문 길이 | 블록당 2000자 제한 → `chunk()`/`bodyFromBlocks()` 로 쪼개고 되돌림 | `text` 컬럼 하나, 제한 없음 |
| 상태 | 셀렉트 필드의 문자열 | `draft`/`approved`/`published`/`dropped` — 코드가 값을 강제 |
| 중복 방지 | 없음(사람이 눈으로 확인) | `idx_dev_logs_date_repo`(날짜+레포), `idx_velog_posts_url`(url) 유니크 인덱스 |

가장 크게 준 건 **2000자 청크 문제가 통째로 사라진 것**이다. 원래 devlog 레포에는 청크로
나누고 되돌리는 두 함수가 정확히 역함수인지 검증하는 테스트가 따로 있었다 — 한쪽만 고쳐서
발행된 글 뒷부분이 조용히 잘린 적이 있어서였다. Supabase 는 텍스트 컬럼에 길이 제한이
없으니 그 클래스의 버그 자체가 없어졌다.

유니크 인덱스는 노션에서 사람이 하던 일(같은 날 같은 레포를 두 번 넣지 않기, 같은 글을 두 번
만들지 않기)을 DB 제약으로 옮긴 것이다. `insertDevLogs` 가 같은 (date, repo) 를 다시 넣으면
DB 가 막고, 수집을 재실행해도 중복이 안 생긴다.

### 워크플로 4개 → 1개

노션 시절에는 collect·draft·publish·sync 가 깃액션 파일 네 개였다. 합치면서 하나로
줄였다 — [`devlog.yml`](.github/workflows/devlog.yml) 이 `github.event.schedule` 문자열을
보고 어느 단계인지 고른다.

```yaml
case "$SCHEDULE" in
  "0 12 * * *") task=collect ;;
  "0 1 * * 0")  task=draft ;;
  "0 22 * * *") task=publish ;;
  "0 13 * * *") task=sync ;;
  *) task="${INPUT_TASK:-collect}" ;;
esac
```

`schedule` 트리거가 여러 크론 식을 가질 수 있다는 걸 이용했다 — 워크플로 파일 하나에 네
`cron:` 항목을 두고, 실행될 때 GitHub 가 *어느 식이 이번 실행을 깨웠는지*를
`github.event.schedule` 로 알려준다. `workflow_dispatch` 로 수동 실행할 때는 이 값이 없으니
입력 드롭다운(`task`)으로 받는다.

**크론은 UTC 다.** 워크플로 상단 표에 KST 시각을 적어뒀지만 실제 `cron:` 값은 UTC 로
변환한 것이다. 시간을 바꿀 때 이 변환을 빠뜨리기 쉽다. 그리고 GitHub 크론은 부하가 크면
수십 분 늦게 돈다고 공식 문서에 적혀 있다 — `collect` 가 21시가 아니라 21시 40분에 돌아도
당일자 커밋은 그대로 다 잡히니 문제가 없지만, `publish` 가 늦게 돌면 "7시에 나갔어야 할
글"이 늦게 나갈 수 있다는 걸 알고 있어야 한다.

### velog 쓰기가 비공식이라, 실패를 시끄럽게 만든다

velog 는 공식 쓰기 API 가 없다. 로그인 쿠키(`access_token`)를 그대로 GraphQL 뮤테이션에
실어 보내는 방식이라 두 가지가 언제든 깨질 수 있다 — 쿠키가 만료되거나, velog 가 프론트엔드
구조를 바꾸거나.

이런 연동은 **조용히 멈추는 게 제일 위험하다.** 발행 승인만 계속 쌓이고 아무것도 안
올라가는 상태를 몇 주 뒤에야 알아채는 식이다. 그래서 `publish` 는 실패를 숨기지 않는다.

- 실패한 글의 `error` 컬럼에 이유를 남긴다 — 화면에 빨갛게 뜬다
- `/api/cron/devlog` 는 발행 실패가 하나라도 있으면 `ok:false` 를 돌려준다
- `devlog.yml` 은 `ok:false` 를 받으면 `exit 1` 로 끝나 GitHub Actions 가 빨갛게 표시된다

```ts
// /api/cron/devlog/route.ts
const failed = body.task === "publish" && (result as { failed?: unknown[] }).failed?.length;
return NextResponse.json({ ok: !failed, task: body.task, result });
```

읽기(`sync`, 역동기화)는 인증 없이 되는 공개 API 라 이 위험이 없다. 쓰기에만 이 방어가
붙는다.

### 왜 아이폰 설정 디자인인가

이 백오피스는 원래 데스크톱에서 키워드를 찾고 글을 쓰는 도구였다. 하지만 승인·복사처럼
짧게 끝나는 조작은 대부분 출퇴근 중 아이폰에서 하게 됐다 — devlog 초안 승인, 쓰레드 후보
고르기, 방문 후기 인터뷰 답하기 전부 몇 초짜리 조작이다. 데스크톱 레이아웃을 화면만 줄여
쓰면 표는 옆으로 잘리고 버튼은 손가락보다 작다.

그래서 아이폰 "설정" 앱의 인셋 그룹 리스트를 그대로 가져왔다. 화면 폭에 따라 같은 메뉴
데이터를 다른 틀에 넣는다.

- **모바일** — 홈이 메뉴 목록이고, 항목을 누르면 그 화면으로 이동. 상단 바(`TopBar`)가
  뒤로가기와 제목을 맡는다. iOS 처럼 본문 맨 위 큰 제목이 스크롤로 가려진 뒤에야 상단 바
  가운데에 작은 제목이 나타난다.
- **태블릿·PC** — iPad 설정의 스플릿 뷰. 왼쪽 `Sidebar` 에 같은 메뉴가 항상 떠 있고
  오른쪽에 화면 본문이 함께 보인다.

메뉴는 [`src/components/Nav.tsx`](src/components/Nav.tsx) 의 `NAV_GROUPS` 배열 하나다.
`Sidebar`·홈 목록·`TopBar` 의 제목(`titleFor`)이 전부 이 배열을 본다. 항목을 추가할 때
세 군데를 따로 고치면 어느 하나가 빠지기 쉬운데, 배열 하나로 묶으면 그럴 일이 없다.

**기존 페이지의 JSX 는 건드리지 않았다.** `write/`, `settings/`, `threads/`, `analytics/`
등은 예전부터 `card`·`page-title`·`hint` 같은 클래스명을 쓰고 있었다. 그 클래스를
`globals.css` 에서 iOS 스타일로 다시 정의하는 쪽을 택했다 — 페이지마다 새 컴포넌트로
바꿔 쓰면 화면 수만큼 회귀 위험이 생기고, 그 화면들은 이번 작업의 대상이 아니었다.
클래스 이름은 그대로 두고 그 이름이 그리는 방식만 바꾸면, 각 페이지는 코드 한 줄 안
바뀌어도 새 디자인을 입는다.

`:has` 트릭 하나가 이 방식의 대표적인 예다. 아이폰 설정의 그룹 리스트는 선택된(파란) 행
위아래로 구분선이 사라진다. 선택된 행 자신의 구분선은 `.ios-row.active::before` 로 쉽게
지우지만, **바로 다음 행의 구분선**은 그 행 스스로는 자신이 활성 행 다음이라는 걸 모른다.
형제 선택자로 "활성 행을 가진 li 바로 다음 li" 를 골라야 한다.

```css
.ios-list > li:has(> .ios-row.active) + li > .ios-row::before {
  border-color: transparent;
}
```

`:has()` 를 못 읽는 브라우저가 있으면 이 규칙 **하나만** 무시되고 나머지 스타일은 멀쩡해야
한다. 그래서 다른 구분선 규칙(`.ios-list > li + li > .ios-row::before`)과 합쳐 쓰지 않고
별도 규칙으로 뗐다 — CSS 는 선택자 목록 중 하나라도 못 읽으면 그 규칙 전체를 버리기 때문에,
한 선택자에 묶어 뒀으면 지원 브라우저에서도 기본 구분선 규칙까지 같이 날아갔을 것이다.

## 18. 티스토리 — 단가보다 유입, 글보다 키워드

티스토리에 85편을 초안으로 쌓아 뒀는데 유입이 0이었다. "블로그명이 n잡러 프리랜서라서
그런가" 하는 의심이 먼저 나왔지만, 유입은 블로그명이 아니라 검색어로 온다 — 이름을
바꿔봤자 아무것도 안 바뀐다. 방향을 튼 건 "단가가 높아도 들어오는 사람이 없는 게
문제"라는 지적이었다. 그동안 단가(수익 잠재력) 순으로 시드를 골라 왔는데, 단가는 유입이
있어야 의미가 있는 지표다. 순서가 거꾸로였다.

### 원인 셋

- **시드가 서로 다른 주제였다.** 연말정산·실업급여·자동차보험·주택청약처럼 12개가 다
  달랐다. 블로그 하나가 뭘 다루는 곳인지 신호가 없으니, 검색엔진도 사람도 "이 블로그는
  뭐 하는 곳"이라고 답할 수 없었다.
- **레드오션만 골랐다.** 월 검색 1,000 이상·수익 잠재력 순으로 뽑으면 이미 세무사·
  보험사·정부 사이트가 상위를 잡고 있는 키워드만 남는다. 새 블로그가 끼어들 자리가
  없었다.
- **한 편도 발행되지 않았다.** 초안 85편이 전부 승인 대기로 쌓여만 있었다. 초안이
  아무리 많아도 발행 전까지는 유입에 0을 더한다.

세 문제가 겹쳐서, 애초에 검색엔진에 "이 블로그가 뭘 잘 아는지" 알려줄 신호 자체가 없었다.

### 결정 — 주제를 하나로 좁히고, 2주는 글을 쓰지 않는다

티스토리 주제를 **n잡러·프리랜서의 돈 관리**(세금·신고 / 보험·연금 / 지원금·수당) 하나로
묶었다. 블로그명은 그대로 둔다 — 문제가 아니었던 걸 고쳐봤자 원인은 그대로 남는다.
기존 글 85편은 삭제했다(백업은 이 저장소가 아니라 사용자 데스크톱에 있다). 레드오션
키워드로 쓴 글을 그대로 두면, 나중에 좁힌 주제와 안 맞는 글이 섞여 신호를 다시 흐린다.

그리고 앞으로 **2주 동안은 글을 쓰지 않는다.** 대신 채널별 시드 전체를 매일 조회해
`keyword_pool` 테이블에 쌓는다(`src/lib/keywordPool.ts`, 깃액션 `keywords.yml` 06:00
KST). 키워드 조회 한 번은 그날의 스냅샷일 뿐이라 "이 키워드가 며칠째 꾸준히 나오는지"를
알 수 없다 — `keyword_pool` 은 upsert 로 같은 키워드가 다시 보이면 `seen_count` 를
늘리고 `last_seen` 을 갱신해서, 우연히 한 번 걸린 것과 꾸준히 나오는 것을 구분한다.
2주치가 쌓이면 그걸 보고 실제로 쓸 글감을 정한다. 그동안 **매일 블로그 초안 워크플로
(`daily-post.yml`)는 꺼 둔다** — 지금 기준으로 글을 계속 만들면 어차피 다시 걷어낼
글이 쌓일 뿐이다.

### 키워드 선택 기준을 뒤집는다

이전 기준(월 검색 1,000 이상, 수익 잠재력 순)은 **이미 자리 잡은 블로그**에 맞는
기준이었다. 새 블로그는 순위표 맨 위를 두고 경쟁할 처지가 아니다. 그래서 뒤집었다.

- **최소 검색 300** — 1,000 이상만 보면 처음부터 레드오션만 남는다.
- **광고 흡수율 낮은 순** — 광고주가 돈을 안 쓰는 키워드는 정보성 검색이라는 뜻이고,
  그런 검색이라야 블로그 글이 끝까지 읽힌다.
- **긴 키워드(질문형) 우선** — 짧은 키워드는 이미 누군가 잡고 있을 확률이 높다. 긴
  키워드는 경쟁이 적고, 질문형이면 글의 답이 뭐가 돼야 하는지도 이미 정해져 있다.

여기에 티스토리 자체의 성격도 반영했다. **티스토리는 네이버가 아니라 구글·다음에서
유입된다** — 지금까지 이 문서에 적은 광고 흡수율·모바일 비중 같은 지표는 전부 네이버
검색광고에서 나온 것이라 네이버 기준 수요를 보여줄 뿐, 실제 유입 경로인 구글·다음의
검색 결과 순위는 알려주지 않는다. 그래서 지표만으로는 못 채우는 것들을 방법으로 남겨
둔다 — 시즌 키워드는 검색이 몰리기 4~6주 전에 선점하고, 계산기 같은 도구형 글은 체류
시간이 길고 다른 글이 인용하기 쉽고, 청약 일정처럼 자기만의 1차 소스가 있으면 아무도
베낄 수 없는 글이 되고, 발행한 글은 쓰레드로 밀어 초기 유입을 만들고, GA4·서치콘솔로
실제 유입을 재서 다음 시드를 고른다.

### 블로그 글이 채널 둘로 갈린다

네이버와 티스토리를 하나의 "블로그" 갈래로 묶어 다뤄 왔는데, 이제 주제부터 다르니
같은 시드·같은 글 목록으로 두는 게 오히려 어색하다. `posts.channel`
(`supabase/migrations/20260925020000_channels_keyword_pool.sql`)을 추가해
`naver`·`tistory` 로 나눴다 — 기존 글은 전부 (예전 기준으로 만들어진) 티스토리용이라
기본값을 `tistory` 로 뒀다. 글 목록 화면도 채널 세그먼트로 나눈다.

매일 초안 크론도 채널별 job 하나씩, **채널당 하루 1편**으로 정리한다. 예전에는 Gemini
키 하나당 글 하나를 만들어서 키가 늘수록 그날 발행량도 늘었는데, 이건 무료 쿼터를 남김
없이 쓰려던 절충이었지 주제 전략은 아니었다. 채널이 갈리고 시드도 주제별로 좁힌 지금은
키 개수만큼 글이 늘어나는 쪽이 오히려 "한 채널이 하루에 여러 주제를 동시에 건드리는"
문제를 새로 만든다. 채널당 하루 1편으로 고정한다.

시드도 코드 상수(`DEFAULT_SEEDS`)에서 설정 화면 **"시드 키워드 — 채널별"** 로 옮겼다
(`src/lib/seeds.ts`, 값은 `settings` 테이블의 `seeds_naver`/`seeds_tistory`, 비어
있으면 코드의 기본값을 쓴다). 시드가 코드에 있으면 바꿀 때마다 배포가 필요했다 — 오늘
같은 결정(시드를 통째로 갈아엎는 것)을 설정 화면 값 하나로 끝낼 수 있어야, 다음에 또
방향을 바꿀 때도 배포 없이 바로 반영된다. 네이버 기본
시드는 임시로 여행·데이트 정보성 키워드를 채웠다 — 네이버 블로그는 맛집 후기가
주력인데, 방문한 곳의 후기는 방문 후기 갈래가 맡고(사진·인터뷰 없이는 쓰지 않는다는
원칙 때문에), 키워드로 자동 조사해 쓰는 글은 안 가본 가게 후기를 지어낼 수 없으니
정보성 주제만 다룬다. 네이버도 2주 뒤 키워드 풀을 보고 다시 정한다.

### 워크플로는 전부 러너가 직접 돈다

`daily-post.yml` 은 그동안 배포본의 `/api/cron/daily-post` 를 curl 로 불렀다 — 판단
로직을 서버에 몰아 두면 워크플로가 한 줄로 끝난다는 장점이 있었지만, 그 대가로 머지·
배포·`CRON_SECRET` 이 먼저 갖춰져야 첫 실행이 됐다. 제휴 후보 워크플로
(`affiliate-picks.yml`)·개발 로그(`devlog.yml`, 17장)는 이미 러너 안에서 소스를
체크아웃해 직접 돌리는 방식으로 옮겨져 있었다. 키워드 모으기(`keywords.yml`)도 처음부터
그 방식으로 만들고, `daily-post.yml` 도 다시 켤 때 같은 방식으로 맞춘다 — DB·API
시크릿만 레포에 있으면 이 파일이 있는 브랜치에서 바로 돌고, 배포 여부와 무관해진다.
크론 라우트(`/api/cron/daily-post`, `/api/cron/devlog`)는 지우지 않고 남겨 뒀다 — 화면
버튼이나 나중에 다른 자동화가 쓸 수 있는 통로라, 워크플로가 안 쓴다고 코드까지 없앨
이유는 없었다.

쓰레드 제휴 후보 워크플로는 끈다. 방향이 바뀌어 나중에 인스타그램과 같이 다시 설계할
예정이라, 지금 그대로 계속 돌리면 새 설계와 안 맞는 결과만 쌓인다. 코드와 화면
(`/threads`)은 지우지 않고 남겨 둔다.

### 아이폰에서 표가 깨진다

글 목록을 아이폰 폭에서 보면 제목 열이 좁아 제목이 세로로 한 글자씩 쪼개져 읽을 수
없었다. 키워드 탐색·경쟁 분석 표는 열끼리 비교하는 게 목적이라 표를 유지하고 최소
폭 + 가로 스크롤로 두지만, 글 목록은 한 행이 글 하나를 온전히 보여주면 되는 목록이라
표일 필요가 없다. 15장에서 들여온 아이폰 "설정" 리스트 행으로 바꿨다 — 제목 한 줄,
채널 배지, 발행 상태를 한 행에 세로로 쌓으면 폭이 좁아도 제목이 줄바꿈될지언정
쪼개지지는 않는다.

### 채널의 뜻을 바로잡는다

앞 절에서 "네이버와 티스토리는 주제가 다른 별도 채널"이라 쓰고 네이버 쪽에 여행·데이트
키워드 시드를 채워 넣었다. 하루 만에 뒤집었다 — **채널이 왜 둘인지**를 잘못 짚었다는
지적을 받았다.

문제는 이거였다. 방문 후기 갈래(`visit_posts`, `/visit`)가 이미 있었고, 사진 → 분석 →
인터뷰 → 초안이라는 파이프라인으로 네이버 블로그에 올릴 글을 만들고 있었다. 그런데 새로
만든 네이버 키워드 시드도 결국 네이버 블로그에 올릴 글이 목적이다 — 같은 발행처를 겨냥한
파이프라인이 하나의 저장소에 둘 생긴 것이다. 티스토리는 "키워드로 쓴다"가 그 채널의
정체성이 맞지만, 네이버는 애초에 갈래를 가르는 축이 "네이버냐 티스토리냐"가 아니라
"키워드에서 출발하냐 사진에서 출발하냐"였다.

원칙과도 정면으로 부딪혔다. 방문 후기 절에는 이미 "사진에도, 카카오 조회에도, 인터뷰에도
없는 것은 쓰지 않는다 — 안 가본 가게 후기는 지어내지 않는다"고 적어 뒀다. 네이버 키워드
시드로 자동 생성한 글은 시드가 "제주도 여행 코스"든 "브런치 카페"든 결국 안 가본 곳을
검색 결과만 보고 쓰는 것이다. 정보성 주제로 좁혀 맛집 후기 자체는 피했다 쳐도, 네이버
블로그는 맛집 방문 후기가 주력인 채널이라 정보성 글이 채널의 성격과 어긋났다. 방문 후기가
네이버다 — 키워드 네이버 채널은 그 자리를 두 번 만든 것이었다.

**걷어낸 것.** 프론트에서는 키워드 탐색 화면의 채널 세그먼트(네이버/티스토리 탭)와 글
목록의 채널 세그먼트·배지를 뺐다. 백엔드에서는 `listPosts`·`listKeywordPool`·
`countKeywordPool`·`collectKeywords`·`writeDailyPost` 등 `channel` 인자를 받던 함수들에서
그 인자를 뺐다. `src/lib/seeds.ts` 의 `Channel` 타입·`CHANNELS`·`DEFAULT_SEEDS` 의 `naver`
키도 없앴다 — 시드는 이제 문자열 배열 하나(`DEFAULT_SEEDS: string[]`)다. 워크플로에서는
`keywords.yml`·`daily-post.yml` 의 채널 입력(`workflow_dispatch.inputs.channel`)과
`daily-post.yml` 의 채널 매트릭스(`strategy.matrix.channel`)를 뺐다. `posts.channel` ·
`keyword_pool.channel` 컬럼 자체는 남겨 뒀다 — 기존 행이 이미 그 값을 갖고 있고, 컬럼
이름이 틀린 게 아니라 "이 컬럼이 두 값을 다 가진다"는 전제가 틀렸을 뿐이다. 이제 이
컬럼들은 늘 `'tistory'` 만 쓴다.

**GPT 로 바꾼 이유.** 이 정정에 맞물려 하나 더 정리했다 — 방문 후기의 사진 분석·본문
생성 모델을 Gemini 에서 OpenAI(GPT, 기본 `gpt-5.5`)로 바꿨다. 네이버·티스토리가 하나의
"블로그" 채널이 아니라는 게 분명해지면서, 두 갈래가 서로 다른 모델을 쓸 이유도 같이
분명해졌다 — 방문 후기는 **사진 인식**(메뉴판 OCR, 음식 식별, 영수증 판독)이 결과물
품질을 좌우하고, 티스토리는 **텍스트 생성**(제목·본문·FAQ)이 좌우한다. 하나의 모델로 둘 다
감당하게 하기보다, 갈래마다 강점이 맞는 모델을 붙이는 쪽을 택했다.

새 호출부는 [`src/lib/openai.ts`](src/lib/openai.ts) 하나다. Chat Completions +
Structured Outputs(`json_schema`, `strict`)를 쓴다. strict 스키마는 규칙이 셋이다 — 모든
객체에 `additionalProperties: false`, `properties` 의 키는 전부 `required` 에 있어야
하고, 없어도 되는 값은 `type: ["integer", "null"]` 처럼 `null` 을 허용해 표현한다.
`visit.ts` 의 스키마를 이 규칙에 맞춰 고쳤다 — 메뉴 항목의 `price` 는 예전엔 없어도 되는
필드(`required: ["name"]`)였지만, strict 에서는 필드를 `required` 에 넣고 값 쪽을
`["integer", "null"]` 로 열어 "가격을 못 읽었다"를 표현한다. 영수증도 마찬가지로 사진이
아예 없으면 `receipt` 자체가 `null` 이다.

gpt-5 계열은 `temperature` 를 받지 않는다(넘기면 400 이 난다) — Gemini 호출에서 "사실
추출이니 온도를 낮춘다"고 걸어 뒀던 값을 뺐다. 날조 억제는 이제 프롬프트와 strict 스키마가
맡는다. 토큰 상한도 `max_tokens` 가 아니라 `max_completion_tokens` 로만 받는다.

**첫 수집 결과에서 보인 것.** `keywords.yml` 이 처음 돈 결과, 티스토리 시드 12개에서
키워드 365개가 모였고 그중 긴 키워드(질문형에 가까운 것)가 108개였다. 하루치 스냅샷이라
아직 주제를 정할 단계는 아니지만, 벌써 눈에 띄는 무리가 하나 있다 — 광고 흡수율 0%인
"계산기" 검색어들이다. 프리랜서3.3%계산기, 사업소득세계산기, 퇴직소득세계산기 같은
키워드가 나란히 걸렸다. 이 장 앞부분에서 "계산기 같은 도구 글은 체류시간이 길고 다른
글이 인용하기 쉽다"고 방법으로만 적어 뒀던 것이, 실제 데이터로 처음 확인된 셈이다. 2주를
다 채우기 전에도 다음 시드 후보 하나는 이미 나와 있다.
## 19. 단가와 유입을 같이 본다 — 애드센스 구조, 티스토리 검색 결과, 데이터 루프

18장에서 시드를 하나로 좁히고 키워드 기준을 뒤집었다. 그 기준으로 2주간 키워드를 모으는
중에 세 가지가 더 걸렸다 — 애드센스가 실제로 어떻게 돈을 주는지, 그 기준에 구멍이 있다는
지적, 그리고 "고른 키워드가 실제로 경쟁이 될 만한가"를 확인할 방법. 이번 장은 그 세
가지와, 그걸 닫으려고 만든 유입 데이터 루프를 정리한다.

### 애드센스는 클릭이 아니라 노출로 번다

애드센스 구조를 다시 봤다. **2024년 3월부터 애드센스는 클릭당(CPC)이 아니라 노출당
(CPM)으로 지급된다.** 수익은 대략 `페이지뷰 × 페이지 RPM`이고, RPM은 광고주가 그 주제에
얼마를 쓰는지로 정해진다. 국내 블로거들이 집계한 값으로는 생활 주제가 1,000~2,500원,
금융·세금 주제가 10,000~30,000원 선이다 — 애드센스가 공식으로 공개하는 수치가 아니라
추정치라는 점은 그대로 남는다.

이게 세금·돈 주제를 계속 미는 근거다. 18장에서 걷어낸 85편이 0원이었던 것도 다시 보면
주제 탓이 아니었다 — **발행이 0편이라 페이지뷰 자체가 0이었다.** 수익이 없다는 사실만으로
"이 주제는 안 된다"고 결론 내리면, 발행량이라는 다른 변수를 놓치고 애꿎은 주제를 버리게
된다.

### 키워드 기준의 구멍 — 흡수율만 보면 광고주가 없는 키워드가 위로 온다

"애드센스는 클릭으로 버는데 흡수율 0%면 애초에 광고가 없다는 뜻 아니냐"는 지적을 받았다.
맞는 말이었다. 18장의 기준(`광고 흡수율 낮은 순`)은 "광고가 상단을 얼마나 가져가는가"만
보고, "그 주제에 광고주가 있는가"는 보지 않는다. 흡수율이 낮은 이유가 둘로 갈리는데, 그
기준은 둘을 구분하지 못한다.

- 정보성 검색이라 광고주가 굳이 클릭을 사지 않는 경우 (원하는 경우)
- **애초에 그 키워드에 붙는 광고주 자체가 없는 경우** (원치 않는 경우 — 노출 지급 구조에서는
  사람이 와도 돈이 안 된다)

실제로 "실수령액계산기"는 월 검색 88,500에 단가 100원이다. 사람은 많이 오지만 단가가
바닥이라 RPM도 바닥일 수밖에 없다. 흡수율 하나만 보면 이런 키워드가 순위 맨 위로 온다.

기준을 고쳤다. **흡수율 2% 미만(검색 상단을 광고가 안 가져감) 이면서 단가 2,000원
이상**인 후보만 남기고, 그 안에서 `검색량 × 단가` 순으로 정렬한다. 한 단어짜리는 뒤로
민다 — 이미 누군가 잡고 있을 확률이 높고 검색 의도도 흐릿하다. 두 조건(낮은 흡수율 + 높은
단가)을 AND 로 걸어야 "사람도 오고 광고주도 있는" 자리만 남는다. README **뭘 보고
고르나** 절에 반영했다.

### 티스토리 기준으로 실제로 확인했다

기준을 고쳤다고 바로 믿을 수는 없어서, 초기 글감 후보 14개로 실제 검색 결과를 긁어
확인했다. 구글은 스크립트 요청을 막아서(9장의 네이버 SERP 파싱과 달리 구글은 자동화
탐지가 더 촘촘하다) 못 읽었고, 대신 **다음 모바일 통합검색** 1페이지를 읽어 결과 사이트가
티스토리인지(자기 도메인으로 연결한 티스토리 포함) 확인했다.

14개 중 7개에서 1페이지에 티스토리가 있었고, "프리랜서 종합소득세"는 1위 자체가
티스토리였다. 상위를 채운 건 세무 서비스 사이트·정보 모음 사이트·오래된 커뮤니티 글·
워드프레스 수익 블로그였다 — 세무사·정부 사이트가 꽉 잡고 있을 거라는 예상과 달리
경쟁이 약했다. "3.3% 환급"은 결과 1페이지에 계산기 도구 사이트가 있었다. 검색자가
계산 결과를 바로 원한다는 뜻이라, 정보 글이 아니라 **계산기 글**로 방향을 정했다(아래).

구글 결과는 이번엔 확인하지 못했다. 봇 요청을 막는 걸 우회하지 않기로 한 원칙(5장, 12장과
같은 선)은 여기서도 유지하고, 대신 **서치콘솔로 실제 구글 순위를 받는 쪽**을 택했다 —
바로 다음 절의 데이터 루프가 그 답이다.

### 유입 데이터 루프를 닫는다

GA4 하나로는 "들어온 뒤"만 보인다. 어떤 검색어로 몇 번 노출됐고 몇 위인지는 서치콘솔에만
있다. 다음 글감을 고르려면 결국 이 데이터가 필요해서 GA4 옆에 서치콘솔을 붙였다.

**인증은 서비스 계정 하나로 묶었다.** 서치콘솔도 GA4와 같은 방식(서비스 계정, OAuth
동의 불필요)으로 인증되므로, 같은 키를 GA4에는 편집자로, 서치콘솔에는 전체 권한으로
등록해 두 API를 같이 연다. 키를 두 벌 관리할 이유가 없다. `src/lib/gsc.ts`가 이
서비스 계정으로 `searchAnalytics.query`를 부른다 — GA4용 `google-auth.ts`의 토큰
캐시·서명을 그대로 재사용한다.

매일 09:30 KST 깃액션이 **최근 7일을 다시 덮어 쌓는다**(`search_daily`·`traffic_daily`·
`event_daily`). 하루치를 한 번만 받지 않는 이유는 서치콘솔 데이터가 확정되기까지
2~3일 걸리기 때문이다 — `dataState: "all"`로 잠정치를 먼저 받고, 며칠 지나 확정치로
갈아끼우려면 그 구간을 매일 다시 조회해야 한다. 개발 로그(17장)에서 이미 검증한
"upsert로 겹쳐 쌓기" 패턴을 여기서도 그대로 쓴다.

성과 화면에 다섯 가지를 더했다.

| 항목 | 내용 |
|---|---|
| 검색 노출·클릭·세션 | 서치콘솔 노출·클릭과 GA4 세션을 나란히 |
| 기회 검색어 | 노출은 되는데 5~30위인 검색어 — 다음 글감 후보 |
| 유입 경로 | 어디서 왔는지. 네이버는 서치어드바이저에 API가 없어 GA4의 소스 값으로만 잡힌다 |
| 페이지별 | 글 단위 조회수·체류시간 |
| 이벤트 | `calculator_use`(아래 계산기 글) 등 |

### 다음 글감은 사람이 고르지 않는다

키워드 풀(18장) 말고 다음 글감을 고르는 순서를 하나 더 만들었다.

1. **글감 큐**(설정 `post_queue`) — 사람이 미리 정해 둔 것부터 쓴다
2. 큐가 비면 **기회 검색어** — 이미 노출은 되는데 순위가 애매한 검색어를 우선한다
3. 그것도 없으면 **키워드 풀**(위에서 고친 새 기준)로 돌아간다

초기 큐 7개는 프리랜서 종합소득세·3.3% 환급 계산기·지역가입자 건강보험료·퇴직금중간정산·
종합소득세신고·세금계산기·양도세기본세율이다 — 앞 절에서 다음 검색으로 경쟁이 약함을
확인한 키워드들이다. 키워드 수집 시드에도 상위 기회 검색어 5개를 더했다 — 네이버 검색광고
지표만으로 시드를 고르면 실제 유입 경로(구글·다음)와 어긋난다는 18장의 지적을, 실제 유입
데이터로 보완하는 셈이다.

**발행은 여전히 사람이 한다.** 티스토리 Open API가 사실상 종료됐고, 브라우저 자동화는
로그인 계정이 카카오 계정과 겹쳐서 — Gemini 키·GA4·애드센스가 걸린 계정을 자동화가
잘못 건드리면 백오피스 전체가 같이 위험해진다(14장에서 이미 같은 이유로 구글 로그인
자동화를 접었다). 자동 선택까지만 만들고 마지막 버튼은 사람이 누른다.

### 계산기 글

"3.3% 환급"에서 확인한 계산기 수요에 맞춰 계산기 글 두 개를 만들었다.

- **3.3% 환급 계산기** — 원천징수·실수령액·종합소득세 환급 추정. 2023년 귀속분부터
  적용되는 누진세율표를 쓴다
- **세금계산기** — 부가세·종합소득세

계산을 누르면 GA4 `calculator_use` 이벤트를 `calc_type`과 함께 쏜다 — 어떤 계산기가
실제로 쓰이는지 봐야 다음 계산기 글감도 고를 수 있다. 결과는 추정치라는 고지를 붙인다 —
세율표·공제 항목이 매년 바뀌고, 개인 상황(부양가족·소득공제)까지 반영한 정확한 세액이
아니다.

### GA4 향상된 측정은 이미 다 켜져 있었다

이벤트를 새로 추적하려고 GA4를 열어 보니 **향상된 측정(Enhanced measurement)이 이미
전부 켜져 있었다** — 스크롤·이탈 클릭 같은 기본 이벤트는 손댈 게 없었다. 새로 건 건
`calculator_use`뿐이다.

주요 이벤트로 `calculator_use`·`scroll`을 등록하고, 맞춤 측정기준으로 `calc_type`을
등록하는 걸 **Admin API로 멱등하게** 했다(`scripts/ga4-setup.mjs`) — 이미 등록된
이벤트·측정기준을 다시 실행해도 중복으로 쌓이지 않고 그대로 둔다. GA4 관리 화면을 열어
클릭으로 설정하는 방법도 있지만, 12장에서 속성 생성을 Admin API로 옮기고 나서 배운 대로
API 쪽이 재현 가능하고 실수도 적다. 이 API를 부르려면 서비스 계정이 GA4 속성에
**편집자**로 등록돼 있어야 한다 — 지금까지는 조회만 하면 됐으니 뷰어로 충분했지만,
이벤트·측정기준을 쓰는 이 호출부터는 뷰어 권한으로 403이 난다. README **자격증명**
절의 GA4 권한을 뷰어에서 편집자로 올린 이유다.

### 로컬 시계 8분 오차로 구글 JWT 가 거부됐다

로컬에서 서치콘솔 연동을 확인하는 중에 `invalid_grant`로 토큰 발급이 계속 실패했다.
서비스 계정 JWT는 `iat`(발급 시각)가 서버 시각과 너무 벌어지면 거부되는데, 맥 로컬 시계가
실제로 8분 빨랐다. 배포본과 깃액션 러너는 시계가 정확해서 겪지 않는 문제라 그동안 안
보였을 뿐이었다.

`google-auth.ts`에 재시도 경로를 넣었다. 토큰 요청이 `invalid_grant`로 거부되면, 응답의
`Date` 헤더로 실제 서버 시각과 로컬 시각의 차이(초)를 재서 캐시해 두고, 그 오차를 반영해
`iat`를 다시 서명해 한 번 더 시도한다.

```ts
const serverDate = Date.parse(res.headers.get("date") ?? "");
if (!retried && payload.error === "invalid_grant" && Number.isFinite(serverDate)) {
  const skew = Math.round((serverDate - Date.now()) / 1000);
  if (Math.abs(skew) > 30) {
    clockSkewSeconds = skew;
    return requestAccessToken(sa, scope, true);
  }
}
```

30초 이내 오차는 무시한다 — 매 요청마다 재시도를 걸면 정상 상황에서도 불필요한 왕복이
생긴다. 오차를 프로세스 전역에 캐시해 두므로, 한 번 재서명에 성공하면 그 뒤 요청은 처음부터
보정된 시각으로 나간다.

### 확인된 것

첫 90일 GA4 세션은 **다이렉트 105, 그다음이 리퍼러 17**이었다 — 아직은 검색 유입보다
직접 방문(주소창 입력·북마크·공유 링크)이 크다. 서치콘솔에는 이미 몇 건이지만 노출이
잡혀 있다 — "건강보험료 확인방법"이 47위로 노출된 기록이 있다. 순위는 낮지만 색인은
되고 있다는 뜻이라, 앞으로 이 노출들이 기회 검색어 목록에 얼마나 쌓이는지가 다음 글감
선택의 실제 재료가 된다.
